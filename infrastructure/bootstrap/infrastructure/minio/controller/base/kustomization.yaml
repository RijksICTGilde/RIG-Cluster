apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - deployment-versioned.yaml
  - service.yaml
  - service-versioned.yaml
  - pvc.yaml
  - pvc-versioned.yaml
