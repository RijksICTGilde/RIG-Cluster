apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-storage-versioned
  namespace: rig-system
  labels:
    backup.rig.nl/enabled: "true"
spec:
  storageClassName: csi-hostpath-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
