apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: rig-system
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
    name: api
  - port: 9001
    protocol: TCP
    targetPort: 9001
    name: console
  selector:
    app: minio
  type: ClusterIP