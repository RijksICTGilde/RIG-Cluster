apiVersion: v1
kind: Service
metadata:
  name: forgejo
  namespace: rig-system
  labels:
    app: forgejo
    component: git-server
spec:
  type: ClusterIP
  selector:
    app: forgejo
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
