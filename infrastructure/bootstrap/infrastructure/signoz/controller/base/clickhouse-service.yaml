apiVersion: v1
kind: Service
metadata:
  name: signoz-clickhouse
  namespace: rig-system
  labels:
    app: signoz-clickhouse
spec:
  selector:
    app: signoz-clickhouse
  ports:
    - port: 9000
      targetPort: 9000
      name: native
    - port: 8123
      targetPort: 8123
      name: http
