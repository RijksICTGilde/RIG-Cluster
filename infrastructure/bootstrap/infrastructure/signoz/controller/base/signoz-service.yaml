apiVersion: v1
kind: Service
metadata:
  name: signoz
  namespace: rig-system
  labels:
    app: signoz
spec:
  selector:
    app: signoz
  ports:
    - port: 8080
      targetPort: 8080
      name: http
