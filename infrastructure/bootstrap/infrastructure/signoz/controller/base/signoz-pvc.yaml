apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: signoz-storage
  namespace: rig-system
  labels:
    app: signoz
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
