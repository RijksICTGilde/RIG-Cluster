apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: signoz-clickhouse-storage
  namespace: rig-system
  labels:
    app: signoz-clickhouse
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
