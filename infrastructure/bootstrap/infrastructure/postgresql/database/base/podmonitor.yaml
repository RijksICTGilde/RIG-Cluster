apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: rig-db
  namespace: rig-system
  labels:
    cnpg.io/cluster: rig-db
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: rig-db
  podMetricsEndpoints:
    - port: metrics
