apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # Prometheus RBAC (namespace-scoped for ArgoCD namespaced mode)
  - role.yaml
  - rolebinding.yaml
  # kube-state-metrics RBAC (namespace-scoped)
  - kube-state-metrics-role.yaml
  - kube-state-metrics-rolebinding.yaml
