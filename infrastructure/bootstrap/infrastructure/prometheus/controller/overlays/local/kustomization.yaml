apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  # RBAC: Uses namespace-manager ClusterRole from operations-manager bootstrap
  # No additional RBAC needed - namespace-manager has cluster-wide pod/service/endpoint read access
