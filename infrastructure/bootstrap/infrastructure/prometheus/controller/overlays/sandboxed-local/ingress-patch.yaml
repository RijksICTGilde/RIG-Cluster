apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus
  namespace: rig-system
  annotations:
    cert-manager.io/cluster-issuer: null
spec:
  rules:
    - host: prometheus.sandbox.rijksapp.dev
      http:
        paths:
          - backend:
              service:
                name: prometheus
                port:
                  number: 9090
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - prometheus.sandbox.rijksapp.dev
      secretName: sandbox-wildcard-tls
