apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - deployment.yaml
  # RBAC is managed by Capsule - these files exist for documentation only:
  # - clusterrole.yaml        # Permissions provided by Capsule tenant config
  # - clusterrolebinding.yaml # Bindings provided by Capsule tenant config
