apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rig-redis-data
  namespace: rig-system
  labels:
    backup.rig.nl/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
