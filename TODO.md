- [ ] Twee lijsten die hier niet staan maar wel op ons wachten. **`~/IdeaProjects/zad-cli/TODO.md`** (vastgelegd 31 juli): de api-sync-pijplijn loopt maar de output blijft liggen (PR #34 wacht op review, #32 en #33 kunnen dicht, en de commit-prefix klopt niet omdat `actions/checkout` shallow cloont zodat `origin/main` ontbreekt en alles `chore` heet, waardoor syncs met echte code geen release triggeren), verder het manifestwerk aan de CLI (`-f/--file`, flag-groepen, schema uit de Pydantic-modellen). Drie dingen daarvan zijn upstream bij ons: het portaal moet `cli_callback` plus `state` accepteren en na aanmaken naar de loopback posten, er moet een endpoint komen dat bij projectcreatie een API-key teruggeeft, en attachment-endpoints ontbreken nog. De vraag die daar openstaat welke IdP achter het portaal zit, kunnen wij in één zin beantwoorden. **`~/IdeaProjects/argo-cd/TODO.md`**: branch `rig/ns-sync` op tag v3.3.12 met de cherry-pick van upstream #27528 en twee fixes uit onze eigen review staat alleen nog in een scratchpad-worktree en moet naar de fork gepusht worden, daarna image bouwen in CI en `bootstrap/rig-system/kustomize/overlays/odcn-production/argocd-deployment.yaml` pinnen op `v3.3.12-rig1`. Eerst in `sandboxed-local`, want het upstream image draait `USER 999` en OpenShift kent een eigen UID toe; #25229 bewust nog niet mergen tot de auteur hem op #27528 rebaset.
- [ ] Health-check service (RC-9, af en al opgenomen in `branches-samenvoegen-naar-main`): na merge naar main en uitrol van het image de service aanzetten op mft-tp9, dirmgr met `scheme: http` + `port: 8080` + de twee paden en dirui zonder poort, maar eerst verifiëren of directory-ui `/health/live` en `/health/ready` echt serveert.
- [ ] Bewaking op de VLAM-keten (vlam-wt8): er staat nu niets, waardoor onze proxy een dag lang naar een oud IP-adres van VLAM stuurde zonder dat iemand het zag. Een controle die periodiek een echte aanroep doet en het antwoord beoordeelt, niet alleen of de poort open is, want in dit geval antwoordde de verkeerde bestemming keurig met een geloofwaardige fout. Dit is een concreet geval van de bredere lacune uit `project_service_monitoring_gap`: geen zicht op dienstniveau.
- [ ] Zelfbediening op identiteit dichtzetten in projectrealms (`features/futures/keycloak-sso-bypass-voorkomen.md`), prioriteit boven het punt hieronder: een gebruiker kan nu zelf een wachtwoord zetten en de IdP-koppeling verwijderen, en logt daarna in zonder SSO Rijk. Fase 1 is gebouwd op `branches-samenvoegen-naar-main` (1 augustus, niet uitgerold): `set_required_action_enabled` en `remove_default_role` in `connectors/keycloak.py`, de sleutels `disabledRequiredActions` en `removeFromDefaultRoles` op het `realms:`-item van `sso-only.yaml` en `sso-support.yaml`, toegepast op de create-weg via `_process_realms()` én op de reconcile-weg via `KeycloakManager._ensure_realm_self_service()`. Bewust niet: de account-feature serverbreed uit (knop 3), en niet op `algoritmeregister.yaml` (geen IdP, dus niets te omzeilen) of op de platform-realm. Op 1 augustus in de sandbox geverifieerd: de omzeiling is eerst end-to-end gereproduceerd (de unlink gaf echt 204 en de koppeling was weg), de scharnierende aanname klopt want met de required action uit negeert Keycloak de `kc_action`-URL volledig, beide connector-methodes zijn idempotent tegen live Keycloak, en de reconcile-weg zet een handmatig heropende realm terug dicht via een gewone `:refresh`. Eén aanname sneuvelde: zonder `manage-account` blijft de account console niet read-only maar wordt hij volledig inert (het token verliest de `account`-audience, alle account-endpoints geven 401), wat voor ZAD geen bezwaar is omdat niets in de portal ernaar verwijst. In een tweede ronde ook de create-weg bewezen (wegwerprealm uit beide blueprints), de reconcile-weg op een tweede realm (`tas-yz7`, sso-only), en de lokale flows: aanmaken zoals de invite-flow dat doet levert gewoon een wachtwoord zonder required actions, en die gebruiker logt daarna normaal in, ook in de dichte toestand. Op een echte sso-only realm blijkt een lokaal wachtwoord sowieso waardeloos, want de browser-flow toont geen inlogformulier maar stuurt direct de federatieketen in. De hertoets is opnieuw gedaan op één wegwerprealm waar per meting alleen de knoppen omgingen, en alle claims houden stand in beide richtingen. Niet nagespeeld en dat kan ook niet breken: de HTML-kant van de invite-flow, die Keycloak niet raakt. Wat nog moet: `scripts/keycloak_self_service_report.py` op productie draaien inclusief `rig-platform`, want gebruikers met een openstaande `UPDATE_PASSWORD` raken klem en dat rapport beantwoordt meteen of er lokale break-glass-accounts op de platform-realm staan; daarna de wachtwoord-credentials van federated gebruikers opruimen, want die houden hun omzeiling. Fase 2 staat als ontwerpnotitie in hetzelfde document en wacht op de mailserver: wachtwoord instellen en resetten via OPI met een gemailde token, dat weigert zodra de gebruiker een federated identity heeft. Binnen Keycloak kan dat niet, want `execute-actions-email` en "wachtwoord vergeten" hangen allebei aan diezelfde required action.
- [ ] Account-link van de Keycloak-service zichtbaar maken in de UI (`features/futures/keycloak-account-link-in-ui.md`): de service heeft geen veld voor het koppelen van een SSO-login aan een vooraf aangemaakt account, terwijl `account-link` (automatic, confirm, verify) al volledig werkt in YAML en in de API. Alleen de UI-laag ontbreekt: options-provider, editable, visualizer en een regel in de layout. Let op de volgorde: `automatic` is pas verantwoord als het e-mailadres niet zelf te wijzigen is (identity-field-lock op de reconcile-weg) én de omzeiling hierboven dicht is.
- [ ] OOM auto-tune in eigendom brengen bij een systeemdienst (`plans/oom-auto-tune-deployment-scoped.md`, elf taken): aanleiding is `asses-k2n/pr-450`, dat op 30 juli vastliep op 45Mi en er nog steeds niet uit is. Twee ontwerpfouten stapelen: de `Available != True`-guard in `_analyze_component_resources()` kapt het OOM-pad altijd af (een OOM'end component ís per definitie unavailable), en de nachtelijke sweep schrijft het gedeelde root-limiet per deployment, wat `api` binnen zes seconden van 75Mi naar 45Mi trok. Taak 1 en 2 lossen de storing op en staan los van de rest, dus die kunnen eerst. Daarna het eigenaarschap: een `HookPoint`-enum met een `AFTER_SYNC`-punt, `ServiceKind.SYSTEM` naast `USER` zodat een dienst altijd draait zonder dat een gebruiker hem kiest, en `ServiceType.RESOURCE_TUNING` als eerste bewoner, waarmee de inline `if oom_failures:`-tak uit `project_manager.py` en de hardgecodeerde aanroep in `oom_watcher.py` allebei door dezelfde registryscan gaan. Let op: een hook mag niet zelf committen, anders racen twee diensten op hetzelfde projectbestand. Nog open en niet implementeren: wat auto-tune voortaan met het root-component doet (met rust laten of alleen omhoog); de invariant die daarbij geldt is dat root een deployment nooit mag beletten zichzelf op te hogen.
- [ ] Cross-domain toegang bouwen als service (`plans/cross-domain-access-service.md`, negen stappen; het plan staat nog alleen op `uniform-declarative-platform-services`, commit 9a0aa720, en komt met die branch mee naar main), via een orch ship op basis van diezelfde branch. Vandaag kan een project niet declareren dat een ánder project bij hem binnen mag: de tenant-baseline laat alleen de eigen deployment, het platform en de backup-namespace toe. De service voegt daar per deployment een eigen NetworkPolicy naast, bewust niet ín de baseline, zodat die een platformgarantie blijft waar geen service in schrijft. Strikt targetten is het uitgangspunt: een regel noemt altijd project, deployment, component en poort aan beide kanten, er is geen "heel project" vorm, en de poort staat structureel op de `to`-kant omdat dat in Kubernetes de ontvanger is. De root draagt de betekenis en de deployment alleen het verschil: die verwijst per `name` naar een rootregel en past in de praktijk alleen de deployment van de peer aan. Drie dingen om vooraf te weten: `$defs/deployment-service-config` in `project_v2.json` is een gesloten object en weigert deployment-level config van welke service dan ook (dezelfde klasse fout die dp-bn7 stil blokkeerde), er komt een opruimregel op service-prefix bij omdat deployment-brede manifests vandaag nooit gepruned worden, en de nieuwe haak `contribute_deployment_manifests` moet afgestemd worden met de `HookPoint`-aanpak uit `plans/oom-auto-tune-deployment-scoped.md` zodat er niet twee haakmechanismen naast elkaar ontstaan. Nog open en bewust buiten de eerste versie: goedkeuring door een platformbeheerder op een inbound-regel (het `ApprovalSpec`-mechanisme kan het al, en dit is de enige service die tenant-isolatie versoepelt, dus dat is het overwegen waard vóór productie) en de `{deployment}`-placeholder die de omgevingsnaam spiegelt.
- [ ] Certificaat eenvoudig te vertrouwen maken voor gebruikers (VLAM). Het hóe staat al in sectie 4 van `vlam-gebruikerstest.md`, met de root uit de verbinding halen plus drie env-vars voor curl, Node en Python. Wat ontbreekt: de geverifieerde root als bestand in de repo zodat niemand hem zelf hoeft te extraheren, en een Linux-variant, want daar liggen de paden anders en zullen mensen naar `update-ca-certificates` grijpen. Dat laatste is systeembreed vertrouwen en dat willen we juist niet. Wacht op bevestiging van de SHA-256-vingerafdruk door SSC-ICT of VLAM.
- [ ] E2E-modaaltesten stabiel maken: `tests/e2e/test_backup.py` en `test_edit_wizard.py` vallen willekeurig om zodra ze samen in één run zitten, los slagen ze altijd. De oorzaak is gevonden en het is geen productiefout: de klik op Opslaan levert helemaal géén verzoek op, want htmx gooit een tweede verzoek weg als hetzelfde element nog bezig is met de rerender van het selectveld. `networkidle` is daar geen goede wachtconditie voor, die is al waar voordat htmx klaar is met zijn eigen administratie. Uitgesloten met metingen: de opslag zelf werkt (tien geslaagde saves in dezelfde run), `validate_project_structure` kost 0,000s, en het formulier houdt na de swap gewoon zijn `hx-post` en `hx-ext`. Ik had in `tests/e2e/helpers/edit_modal.py` twee dingen aangepast, wachten op het antwoord in plaats van op netwerkstilte, plus een `_wait_htmx_idle` die wacht tot de klasse `htmx-request` weg is; dat bracht het van 1, 2, 3, 1 fouten naar 1, 1, 0 maar niet naar nul, en die wijziging staat inmiddels niet meer in de boom. Volgende stap: dezelfde wachtconditie ook op het openen van de modal en rekening houden met de blokken die de detailpagina sinds main lui nalaadt, want die vuren verzoeken af terwijl de test al klikt.
- [x] Sleep-mode schreef lege formuliervelden als lege waarden het projectbestand in. GEFIXT 1 augustus (niet uitgerold). Waargenomen op `dimp-r0v`: `match: []`, `waker-component: null`, `title: null` en `description: ''`. Oorzaak, en het is wel degelijk de gewone editables-weg zoals verwacht: een leeg tekstveld komt binnen als `""` en niet als `None` (dat laatste slaat `_write_field` over op `processor.py:366`), de converter maakt daar `None` of `[]` van, en zonder `remove_when_none` valt dat in de `else`-tak die de lege waarde wegschrijft; `description` had helemaal geen converter en schreef de lege string letterlijk weg. Fix: `remove_when_none=True` op `match`, `waker-component`, `title` en `description` in `catalog/sleep_mode/editables.py`, plus een `EmptyToNoneConverter` op `description`. Sleep-mode was de enige die die vlag nul keer gebruikte terwijl hij elders 27 keer staat. Bewust niet op `enabled` en `waker`: dat zijn booleans, de vlag behandelt `False` als leeg, en `waker` heeft default `True` dus uitvinken zou hem stil terugzetten op aan. Regressietest `TestSleepModeEmptyFieldsLeaveNoKeys` in `tests/test_editable_processor.py`, geverifieerd dat hij op de oude code faalt met precies het waargenomen symptoom. Let op bij het schrijven van zulke tests: `process_json_submission` leest GENESTE JSON via padtraversal, geen platte sleutels, en respecteert `virtualize`, dus de ingezonden structuur moet `_services-config/<service>/config/<veld>` genest zijn.
- [ ] Twee restpunten van de sleep-mode-fix hierboven. Ten eerste: eenmaal geschreven blijven die lege sleutels staan, want ze komen als bestaande data weer binnen en `remove_when_none` verwijdert alleen bij een lege inzending. Bestaande projectbestanden hebben dus een opruimactie nodig los van de formulierfix; `attachments: config: []` op een deployment-component in datzelfde bestand heeft dezelfde geur en hoort in dezelfde veeg. Wat GEEN openstaand punt meer is: de betekenis van een lege `match`. Besloten op 1 augustus dat die gewoon nergens op matcht, dus `enabled: true` zonder patronen is een geldige maar inerte toestand en `SleepModeConfig.matches()` blijft zoals hij is. Niet opnieuw ter discussie stellen.
- [x] Kloonstatus (`generation` + `revisions`) als herbruikbaar patroon. GEBOUWD 1 augustus (niet uitgerold): `catalog/shared/revisions.py` met `RevisionAction`, `Revision` en een `CloneState`-mixin, in de geest van `shared/storage.py`. `postgresql-database` en `minio-storage` hebben nu een `config_model.py` dat die mixin componeert (minio ook `enable-versioning`), plus gegenereerde schemafragmenten. Verplichte velden zijn gebaseerd op wat 33 echte revisies in de productiebestanden dragen: alles behalve `superseded_at`. Geverifieerd met een audit over alle 47 productiebestanden na `migrate_to_latest()` in memory: postgresql-database 31 keer OK, minio-storage 3 keer OK, nul validatiefouten. De laag is bewust de keuze van de componerende service; er wordt nergens aangenomen dat het deployment-niveau is.
- [x] Twee blokkades die het kloonpatroon herbruikbaar maakten. OPGELOST 1 augustus (niet uitgerold). Ten eerste `revision_manager`: `_get_service_config` en `_ensure_service_entry` gebruikten een JSONPath op `@.reference`, en drie andere queries (`get_active_revision`, `add_action`, `get_superseded_resources`) hadden elk hun eigen kopie daarvan. Alles loopt nu via `service_entry_name` en `service_entry_config`, dus alle vier de entry-vormen worden gevonden; de drie queries leunen op `get_revisions` zodat de vormafhandeling op één plek zit, en de twee JSONPath-helpers zijn daarmee dood en verwijderd. `_ensure_service_entry` hergebruikt nu een bestaande entry en promoveert een kale string in plaats van een tweede entry toe te voegen; dat laatste zou een duplicaat in de selectieset opleveren en dan wijst `validate_project_structure` het hele bestand af. Ten tweede het schema: `$defs/deployment-service` stond op `additionalProperties: false` met alleen `reference` en `config` (dus geen `name`-record en geen `schema-version`), en `$defs/deployment-service-config` liet alleen `generation` en `revisions` toe. Allebei opengezet tot envelope, met de reden in een `comment`. Omdat de globale schemacontrole daarmee wegvalt, loopt `validate_service_configs` nu ook over de deployment-laag, dus per-service modellen nemen het over. Geverifieerd tegen alle 47 productiebestanden na migratie: nul schemafouten, nul configfouten, en 33 revisies gevonden via de nieuwe lookup.
- [x] Alle services hebben nu een configmodel en een vastgelegd schemafragment. AF 1 augustus (niet uitgerold). `redis` (`acl-key-prefix`), `publish-on-web` (`tls` plus `attachment` op component en het `domains`-goedkeuringsblok op project) en `attachments` (lijst van koppelingen op component, als `RootModel` net als storage) hebben nu elk een `config_model.py` plus fragment. Dertien van de vijftien services zijn gemodelleerd; `namespace-redis` en `platform` blijven bewust behaviour-only. Ook `config_schema_version` rechtgetrokken: de default op de basisklasse was `"1.0"`, dus elke service zonder model adverteerde een contract dat er niet was; die is nu `None` en `tests/test_service_config_schema.py` bewaakt dat model en versie samen reizen. Geverifieerd tegen alle 47 productiebestanden na migratie: nul schemafouten, nul configfouten, en elke service valideert. Drie bestaande tests hadden een achterhaalde premisse (ze gebruikten publish-on-web of minio als voorbeeld van een service zónder model) en zijn omgezet naar `namespace-redis`, telkens met een test voor de omgekeerde kant erbij.
- [x] `validate_service_configs` loopt nu over alle vier de lagen. AF 1 augustus (niet uitgerold). Aanleiding: bij het openzetten van `$defs/deployment-service-config` had ik zelf een gat geslagen, want `persistent-storage` op deployment-component valt onder `additionalProperties` en werd daar dus alleen door dat gesloten object gevalideerd; een onzinsleutel glipte er daarna doorheen. Gemeten en bevestigd, en nu gerepareerd door de deployment-component-laag mee te lopen, met beide vormen die daar voorkomen: een dict gekeyd op servicenaam en, voor de storage-services, een lijst van per-mount-records onder die sleutel. Daarvoor was een contract-uitbreiding nodig: `Service.config_model_for(layer)` met `config_model` als default, want persistent-storage en temp-storage dragen mount-specs op component maar per-mount kloonstatus op deployment-component, en één model kan die twee niet beschrijven. Die twee services overriden hem naar `StorageCloneState` in `catalog/shared/storage.py`, dat op de gedeelde `CloneState` leunt. Geverifieerd: 47 productiebestanden nul fouten, 37 deployment-component-configs worden nu daadwerkelijk gevalideerd, en de regressie wordt gevangen.
- [x] `project_v2.json` opgeruimd nu de servicevalidatie sluit. AF 1 augustus (niet uitgerold). `deployment-component.services` noemde `attachments` en `publish-on-web` bij naam en is nu een generieke envelope (`$defs/deployment-component-service`) die de twee vormen toestaat die daar voorkomen: een record met een config, en een lijst van per-mount-records. Daarmee werden `$defs/publish-on-web-config` en `$defs/attachment-use-entry` wees en zijn ze verwijderd; die kennis zit nu in `PublishOnWebConfig` en `AttachmentsConfig`. De drie tests in `tests/test_attachment_schema.py` die tegen de verwijderde def valideerden zijn meeverhuisd naar `AttachmentUse`, met dezelfde regels. BEWUST BLIJVEN STAAN, met de reden in een `comment` in het schema: de root-`domains`-defs en de v0-boolean `publish-on-web: true`, want `git_monitor.py:137` valideert een bestand rechtstreeks uit git VOOR enige migratie, dus die vormen moeten daar door de poort komen; weghalen zou die beveiligingspoort legacy-bestanden laten afwijzen. Er staan nu nul servicenamen als contract in het schema. Geverifieerd op beide paden: 47 productiebestanden na migratie nul fouten, en rauw exact dezelfde 22 fouten als met het originele schema, dus niets nieuws kapot.
- [ ] BLOKKEREND voor het kunnen afronden van migraties: het projectschema is één bestand voor alle 2.x-versies, terwijl `git_monitor.py:137` een bestand rechtstreeks uit git valideert VOOR enige migratie. Daardoor moet `project_v2.json` elke oude vorm blijven kennen zolang er één bestand op schijf staat dat hem nog draagt, en kan een migratie per definitie nooit afgerond worden; het schema groeit alleen maar. Gemeten op 1 augustus: 30 van de 47 productiebestanden dragen nog de root-`domains:` van vóór v2.5 en 23 nog het `config.keycloak`-restant van vóór v2.3, terwijl beide migraties allang bestaan. Bestanden herschrijven zichzelf namelijk pas bij verwerking (`project_manager.py:2664` slaat op zodra `was_migrated`), en veel projecten worden zelden verwerkt. Dit verklaart ook waarom 22 bestanden de rauwe schemavalidatie niet halen. De richting is bepaald: een bestand declareert zijn versie (`detect_schema_version` geeft die al exact terug) en wordt gevalideerd tegen het schema van díe versie; een migratie levert een nieuwe schemaversie op. Dat patroon bestaat al in deze codebase, alleen op serviceniveau: `<service>.v1.0.json` per service, gegenereerd en drift-gelockt door `opi/services/config_schema.py`. Doe op projectniveau wat services al doen. Let bij het uitwerken op de reden die er nu voor de huidige volgorde staat: valideren vóór migratie voorkomt dat een vijandig bestand dat toevallig schoon migreert onder onze eigen identiteit teruggeschreven wordt (`project_manager.py:2658`). Zolang dit niet af is kunnen de legacy-defs (`domains`, `invites`, de v0-boolean) niet weg.
- [ ] Twee vondsten uit de schema-opruiming, allebei pre-existing. Ten eerste: 22 van de 47 productiebestanden falen de RAUWE schemavalidatie, 21 daarvan op `config.keycloak` (een v2.2-restant dat de migratie verplaatst) en 1 op `components/0/path` als string in plaats van lijst. Dat is niet theoretisch: `git_monitor.py:137` valideert vóór de migratie, dus een wijziging die direct naar git gecommit wordt op die projecten wordt door die poort geweigerd. Uitzoeken of dat pad in productie echt geraakt wordt, en zo ja of de poort na migratie moet valideren of het schema die legacy-vormen moet blijven accepteren. Ten tweede: `$defs/attachment-data-entry` en `$defs/service-config` waren al wees vóór al mijn wijzigingen. `service-config` is dode code. `attachment-data-entry` is dat NIET: het beschrijft de projectcatalogus van attachments (`id`, `filename`, AGE-versleutelde `content`) die vandaag door niets gevalideerd wordt, want de projectlaag delegeert naar de servicemodellen en `validate_service_configs` slaat de entry over omdat de catalogus onder `data` staat en niet onder `config`. Ik heb die def laten staan met een toelichting erin, want weghalen maakt het gat onzichtbaar. De nette oplossing past bij de rest: de catalogus in de attachments-service modelleren, wat vraagt of `data` onder `config` hoort of dat de projectwandeling `service_entry_config` moet gebruiken in plaats van `Project.service_config` (die twee geven nu verschillende antwoorden op dezelfde entry).
- [ ] Een test die een kernmodule herlaadt moet hem herstellen. Opgelost voor `tests/test_secret_key_failclosed.py` op 1 augustus (`importlib.reload(opi.core.config)` herbond `settings` naar een nieuw object terwijl elke module die `from opi.core.config import settings` deed het oude vasthield, waardoor een latere test het verkeerde object patchte en `test_sleep_mode_manifests` omviel in de volle run maar niet los). Openstaand: dit is een valkuil die zich herhaalt, dus overweeg een guard in `conftest.py` die aan het eind van een test controleert of `opi.core.config.settings` nog hetzelfde object is als bij aanvang, en luid faalt als een test dat stilletjes vervangt.
- [ ] Vijf services zijn nog niet klaar voor de nieuwe service-opzet, zie de inventarisatie in `plans/service-migration-inventory.md` (1 augustus). Dat zijn `publish-on-web`, `attachments`, `postgresql-database`, `minio-storage` en `redis`: allemaal dragen ze config in echte projectbestanden zonder configmodel en zonder vastgelegd schemafragment. Let op dat mijn eerste twee inventarisaties op bestandsnamen scanden en daardoor fout waren: `persistent-storage` en `temp-storage` lijken zonder model maar delen er een via `catalog/shared/storage.py` en zijn compleet, en het contract zegt expliciet dat alleen `__init__.py` verplicht is, dus een ontbrekend `config_model.py` is op zichzelf geen gat. Meet via de registry, niet via `ls`.
- [ ] Een service met alleen component-config geeft geen enkele terugkoppeling in de project-brede stap van de bewerk-wizard. Waargenomen op 1 augustus op `dimp-r0v` in `modal-edit-services`: sleep-mode en health-check aangevinkt, alleen de configpagina van sleep-mode verschijnt. Dat is geen bug maar ontwerp, alleen legt niets het uit: `sleep_mode` is `ConfigLayer.PROJECT` en `health_check` is `ConfigLayer.COMPONENT`, en die laatste heeft zelfs helemaal geen `config_form_section`. Het raakt vijf services: `health_check`, `metrics_scraper`, `persistent_storage`, `publish_on_web` en `temp_storage`. Voorstel: in die stap per aangevinkte service die geen project-sectie levert een regel tonen in de trant van "deze dienst stel je per component in", met een verwijzing naar waar dat dan wel kan. Let bij het uitwerken op dat `ServiceDefinition` al een `scope`-veld heeft (`services.py:500`), maar dat dat iets anders lijkt te betekenen dan `ConfigLayer`: keycloak staat op `scope="component"` terwijl zijn configsectie `ConfigLayer.PROJECT` is. Eerst uitzoeken of dat twee begrippen zijn of een inconsistentie.
- [ ] Elke service een uitgebreide beschrijving geven, met een eigen Jinja-template zodat de vormgeving goed kan. Vandaag is er alleen een enkele zin in `ServiceDefinition.description` (`services.py:497` en verder), naast `name`, `icon`, `color`, `scope` en `variables`. Hangt samen met het punt hieronder over servicemetadata naar het servicepakket brengen: als die metadata toch verhuist, is dat het moment om de beschrijving van één regel naar een eigen template te tillen, in de stijl van `keycloak/section-detail.html.j2`. Te bepalen: waar die beschrijving getoond wordt (`/services`, de wizard-keuzestap, de detailpagina, of alle drie), en of er naast de lange tekst een korte blijft voor de kaartjes.
- [ ] Servicemetadata naar het servicepakket brengen: naam, omschrijving, icoon, kleur en variabelen staan nog centraal in `ServiceAdapter.SERVICE_DEFINITIONS`, en `catalog/base.py:237` zegt zelf dat dat "de bron van waarheid blijft tijdens de migratie". Dat is het laatste stuk dat een service niet zelf bezit. `/services` rendert daaruit (`services_router.py:37`) en toont daardoor ook `platform`, dat volgens de enum juist niet door een gebruiker te kiezen is; bij het verplaatsen meteen bepalen of die pagina alleen kiesbare services hoort te tonen.
- [ ] Invites omzetten naar een echte service (`plans/invite-service.md`, geschreven 1 augustus, nog niet gebouwd). Vandaag is het geen service: `ServiceType` kent geen invite-waarde, er is geen package in `opi/services/catalog/`, en de config staat als top-level `invites:` in het projectbestand (`project_v2.json` regel 55, met `$defs/invites` en `$defs/invite`) in plaats van onder het servicepad. De afhandeling ligt verspreid over `InviteManager`, `opi/api/invite_routes.py`, drie extractiemethodes op `ProjectFileHandler` en vier `invite-*.html.j2`-templates. De grootste lacune: er is geen enkele route om een invite aan te máken, alle zeven routes zijn inwisselroutes, dus een invite ontstaat alleen door met de hand YAML te bewerken. Vier projecten gebruiken het vandaag (`asses-k2n`, `dp-bn7`, `mb-docs-helmfile`, `openp-4pw`), dus er hoort een migratie bij. Keycloak-afhankelijkheid: de invite draagt `realm_roles` (in de praktijk `allowed-user`) en kent die toe via `assign_invite_permissions`; de service hoeft de rol niet te controleren want Keycloak bepaalt de toegang, maar moet hem wel kunnen dragen, en het voorstel is een select gevoed door `KeycloakConfig.realm_roles` (`catalog/keycloak/config_model.py:96`) in plaats van vrije tekst. Tweede spoor: de sleutel is nu zelfgekozen en goed raadbaar (`invulhulpen`), dus de link is niet echt geheim; uit te werken zijn een optie om een willekeurige sleutel te genereren (semi-geheim, zoals een niet-vermelde YouTube-video) en een set van X vooraf gegenereerde sleutels om uit te delen, met de zelfgekozen sleutel als optie overeind. Meenemen: `InviteManager.validate_invite` leest `expires_at` maar dat veld staat niet in `$defs/invite` en dat def staat op `additionalProperties: false`, dus dat is een dood pad; kiezen tussen veld toevoegen of code weghalen.
- [ ] `features/service-provider-registry.md` is achterhaald op één punt: regel 25 beschrijft `opi/services/catalog/<service>.py` als één module per service, terwijl het sinds RC-5 een package is met `config_model.py`, `editables.py`, `visualizers.py` en het schemafragment ernaast. Corrigeren en doorverwijzen naar `instructions/services.md`, dat de werkinstructie bevat; het feature-document houdt het waarom.
- [ ] Volledige scan van taalgebruik en één keuze maken: overal Nederlands, of de vertaallaag echt in gebruik nemen. De infrastructuur staat er al en is bedraad, maar wordt niet gebruikt: `opi/locale/` heeft `base.pot` plus `en_US` en `nl_NL` met `.po` en `.mo`, en `core/templates.py:233` installeert de gettext-vertalingen in de Jinja-omgeving via `core/i18n.py` (babel). Alleen is de catalogus leeg, `nl_NL/LC_MESSAGES/messages.po` bevat één `msgid` en dat is de header, terwijl minstens vijftien templatebestanden hardgecodeerde Nederlandse teksten dragen. Er loopt bovendien een derde, parallel mechanisme: invites hebben per invite eigen `nl`/`en`-blokken via `$defs/i18n-text` in `project_v2.json`. Te beslissen: (a) Nederlands als enige taal en dan de gettext-plumbing weghalen in plaats van hem leeg te laten staan, of (b) strings markeren en de catalogus vullen, en dan ook bepalen wat er met die per-invite i18n gebeurt. Wat het ook wordt, de huidige toestand is de slechtste van de drie, want hij suggereert vertaalbaarheid die er niet is.
- [ ] Tijden consequent in onze eigen tijdzone tonen. Het serverdeel is al goed en dat hoeft niet aangeraakt: `Dockerfile:17` zet `TZ=Europe/Amsterdam`, de draaiende pod bevestigt CEST, en de logregels lopen gelijk met de podklok, dus logging staat al lokaal. Het gat zit in de bron van de waarden: zestien plekken in `opi/` berekenen tijd met `datetime.now(tz=UTC)`, dus wat er opgeslagen en doorgegeven wordt is UTC terwijl de weergave Amsterdam moet zijn. Nodig is een omzetting op de weergavelaag (een Jinja-filter of een helper) plus een inventarisatie van welke tijden de UI toont en of die uit een UTC-bron of uit een externe bron met eigen tijdzone komen, bijvoorbeeld ArgoCD, Kopia en Prometheus. Eén bewuste uitzondering laten staan: `services/log_watcher.py:352` gebruikt met opzet een naïeve `datetime.now()` voor de lokale wandklok, met `noqa` en reden erbij.
- [x] `testcontainers` ontbrak in de venv waardoor vijf testbestanden niet konden laden. OPGELOST 1 augustus: `uv sync --all-groups` installeerde `testcontainers`, `docker` en `pytest-timeout`. Daarna bleek de oorzaak dieper te liggen: de reaper-container `testcontainers/ryuk` start niet op Docker Desktop (500 van de daemon) en nam elke containertest mee, 58 errors. `tests/conftest.py` zet nu `TESTCONTAINERS_RYUK_DISABLED` via `setdefault` in de `_orm_pg_container`-fixture, met de reden erbij; de container wordt sowieso door de context manager gestopt, dus de reaper is daar overbodig. `setdefault` zodat CI hem terug aan kan zetten als het daar wel werkt.
- [x] Het hele projectbestand, inclusief wachtwoorden, komt in de logs terecht. GEFIXT 1 augustus (niet uitgerold): `bridge.py` logt nu providernaam plus `sorted(kwargs)`, dus alleen sleutelnamen, en `import logging` plus de logger staan op moduleniveau in plaats van binnen de `try`. Regressietest `TestResolveOptionsDoesNotLeakSecrets` in `tests/test_editables_bridge.py`, geverifieerd dat hij faalt op de oude regel.
- [ ] Vervolg op het log-lek hierboven, want de fix dekte één regel en niet de klasse. Drie andere logregels dumpen een hele dict: `manager/minio_manager.py:95` en `manager/redis_manager.py:363` loggen een compleet serviceconfigblok uit het projectbestand op DEBUG (geen van beide services heeft een `config_model.py`, dus die configs zijn losse dicts en er is niets dat secrets erbuiten houdt), en `generation/manifests.py:278` logt op ERROR de volledige manifest-config, waar wél secret-materiaal in kan zitten. Verder twee structurele vragen: `utils/logging_config.py:59` zet de `opi`-logger hard op `DEBUG` zonder env-knop, dus elke debugregel gaat via stdout naar Loki waar hij veel langer blijft staan dan in `kubectl logs`; en willen we een redactiefilter op sleutels als `password`, `secret`, `token` en `key`, zodat één onbedachte f-string dit niet opnieuw kan doen. Dat laatste is de enige maatregel die de klasse echt dicht, want de drie regels hierboven zijn gevonden met één grep-patroon en dat vindt niet alles.
- [ ] Een uitgeschakelde deployment wordt overal als "Healthy" gerapporteerd, en dat is onwaar: de deployment staat uit. Een component met `disabled: true` krijgt `replicas: 0` (`project_manager.py:5073`) en ArgoCD noemt nul replicas gezond, waarna die waarde ongefilterd doorgaat naar drie plekken: het dashboard (`web/router.py:966-982`, inclusief de banner "Alle projecten zijn gezond"), de deployment-kaart (`_argocd-deployment-card.html.j2:88`, waar de groene Healthy-badge naast de rode badge "Uitgeschakeld" op diezelfde kaart staat) en de V2 API (`api/v2/router.py:164` `_collapse_argo_status`). Slapende deployments (sleep-mode) hebben exact hetzelfde, ook replicas 0, ook Healthy. Voorstel: een afleiding uit het projectbestand, `derive_run_state(project_data, deployment)` in een nieuwe `opi/services/deployment_state.py`, die RUNNING, PARTIALLY_DISABLED of DISABLED teruggeeft en die drie plekken voedt; bij DISABLED toont de badge "Uitgeschakeld" in plaats van de health, bij gedeeltelijk blijft health staan met een chip "N van M componenten uitgeschakeld" ernaast. Twee bijvangsten meenemen: de kaart filtert met `deployment.components | selectattr('disabled')` en ziet dus alleen deployment-niveau, terwijl `extract_deployment_component_disabled` ook de component-definitie als fallback kent, en `health` uit ArgoCD beantwoordt "draait het zoals gevraagd" en niet "is het aangezet". Drie beslissingen vooraf: krijgt `DeploymentStatus` een lid `Disabled` (contractwijziging voor API-consumers), gaat sleep-mode in dezelfde slag mee of apart, en blijft het woord "Uitgeschakeld".
- [ ] Zes commits uit `implementatieplan-sleep-mode-slaapstand-met-wekken` nog samenvoegen in `branches-samenvoegen-naar-main`. De twee uit `uniform-declarative-platform-services` zijn op 1 augustus binnengehaald (`99e8f722`), vooruitlopend op RC-13 omdat taak 11 van dat plan `instructions/services.md` uitbreidt en dat anders aan de onvolledige versie zou gebeuren; dat bestand draagt nu beide helften (`hidden` komt acht keer voor waar het er nul was, 326 naar 475 regels) en de suite bleef groen. Wat nog openstaat is puur documentatie: `plans/oom-auto-tune-deployment-scoped.md` (588 regels, het OOM-plan uit het punt hierboven) plus het VLAM-werk (`vlam.md` +696, `vlam-gebruikerstest.md` nieuw, `vlam-ciso-samenvatting.md`, en een regel in `features/futures/branches-samenvoegen-naar-main.md`). Geen productiecode, dus laag risico. Let op dat RC-12 en RC-13 deze branch als basis hebben en dus op een oudere tip bouwen; hun PR's zullen moeten rebasen.
- [ ] `instructions/services.md` is op 28 juli door twee sessies tegelijk uitgebreid en dat geeft een conflict bij het samenvoegen naar main; beide helften moeten erin, kiezen is fout. Op `uniform-declarative-platform-services` (commit `94d397e7`) staat de sectie "Forms and wizard screens": registratie van een service levert nul UI op, wat `hidden=True` doet (de reden dat sleep-mode nergens in de wizard verschijnt terwijl de service volledig werkt), een tabel per `ConfigLayer` met wat automatisch gaat en wat je met de hand bedraadt, en het verschil tussen Editable en visualizer. Op `branches-samenvoegen-naar-main` (`84571ed7` en verder) staat de `detail_page_sections`-hook, een hooks-overzicht en de alinea over de drie plekken waar een project-level configsectie bedraad moet worden, plus de eis van een user-based sandbox-E2E. De merge-branch bevat het woord `hidden` nergens, dus juist de oorzaak van het sleep-mode-gat ontbreekt daar. Controle achteraf: `grep -c hidden instructions/services.md` op main mag niet nul zijn.
- [ ] Commit-identiteit rechttrekken en bij de bron oplossen. Op `branches-samenvoegen-naar-main` staat `Claude Code` als committer op onder meer `769196d6`, `76f22bb6`, `74fad0e5`, `6448933d` en `efaa9af4`, en op `uniform-declarative-platform-services` staat het als auteur op onder meer `626204bd`, `026ee9ff` en `81e51894`. Controleren met `git log --format='%h|%an|%cn|%s'` en herschrijven vóór de merge naar main, want daarna zit het permanent in de historie. De echte oplossing zit een laag eerder: dclaude (DistributedClaude) zet bij het opzetten van een sessie geen `user.name` en `user.email`, dus elke gespawnde sessie valt terug op de standaard van het image. Daar `git config user.name` en `user.email` op de gebruiker zetten in het bootstrap-pad van `spawn-session`, of `GIT_AUTHOR_*` en `GIT_COMMITTER_*` in de sessieomgeving meegeven, zodat dit niet telkens achteraf hersteld hoeft te worden.
- [ ] RIG-World: de Redis-prefixfix staat daar ongecommit in de werkmap en die map is geen git-repo, dus hij is zo weg. `_key()` voegt nu zelf de dubbele punt toe (`prefix.rstrip(":")` plus `f"{prefix}:{name}"`) omdat wij `REDIS_PREFIX` sinds commit feb0c555 zonder scheidingsteken exporteren en de ACL `~<prefix>:*` eist. Drie dingen die daar nog open staan: de fallback in `config.py` bouwt een URL zonder de ACL-gebruiker (in ZAD niet zichtbaar omdat wij `REDIS_URL` compleet meegeven, daarbuiten wel), `redis_prefix` luistert alleen naar `REDIS_PREFIX` en niet ook naar `APP_REDIS_PREFIX`, en `.env.example` noemt `REDIS_PREFIX` en `REDIS_USERNAME` niet.
