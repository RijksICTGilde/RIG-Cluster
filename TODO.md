# TODO

Wat er nog gemaakt moet worden. Afgerond werk staat in `TODO_DONE.md`,
wachten-op-uitrol in `TODO_NEXT_RELEASE.md`, en bewust uitgesteld in `TODO_FUTURE.md`.

- [ ] Twee lijsten die hier niet staan maar wel op ons wachten. **`~/IdeaProjects/zad-cli/TODO.md`** (vastgelegd 31 juli): de api-sync-pijplijn loopt maar de output blijft liggen (PR #34 wacht op review, #32 en #33 kunnen dicht, en de commit-prefix klopt niet omdat `actions/checkout` shallow cloont zodat `origin/main` ontbreekt en alles `chore` heet, waardoor syncs met echte code geen release triggeren), verder het manifestwerk aan de CLI (`-f/--file`, flag-groepen, schema uit de Pydantic-modellen). Drie dingen daarvan waren upstream bij ons; het middelste is af (RC-51, 7 augustus): `POST /api/v2/projects` maakt een project aan op een SSO-token (`Authorization: Bearer`, geverifieerd tegen de JWKS van de realm) en geeft projectnaam plus API-sleutel terug in de body, met de publieke client `zad-cli` (PKCE, loopback) in de bootstrap-templates; zie `features/cli-project-aanmaken.md`. Daarmee vervalt ook de eerste, want er wordt gekozen voor aanmaken vanaf de CLI zelf in plaats van het formulier in de browser, dus het portaal hoeft geen `cli_callback` plus `state` te accepteren. Blijft open: de attachment-endpoints. De vraag die daar openstaat welke IdP achter het portaal zit, kunnen wij in één zin beantwoorden. **`~/IdeaProjects/argo-cd/TODO.md`**: branch `rig/ns-sync` op tag v3.3.12 met de cherry-pick van upstream #27528 en twee fixes uit onze eigen review staat alleen nog in een scratchpad-worktree en moet naar de fork gepusht worden, daarna image bouwen in CI en `bootstrap/rig-system/kustomize/overlays/odcn-production/argocd-deployment.yaml` pinnen op `v3.3.12-rig1`. Eerst in `sandboxed-local`, want het upstream image draait `USER 999` en OpenShift kent een eigen UID toe; #25229 bewust nog niet mergen tot de auteur hem op #27528 rebaset.
- [ ] Featureverzoeken van Marc (Slack, 1 augustus). **Herijkt op 5 augustus: de kern is af en wat rest zijn vier losse klussen, geen samenhangend plan.** KLAAR: variabele-interpolatie in `user-env-vars` (`$VAR` en `${VAR}`) is met RC-25 meegekomen en staat in `project_manager.py:5466`, waar het wordt opgelost tegen alles wat het component ziet; de eerdere notitie dat `utils/env_vars.py` geen vervanging kende was al fout, en inmiddels is ook de toepassing er. KLEIN GEWORDEN: een herstartknop per component past op de `DeploymentAction`-haak in `services.py`, en die keten is op 5 augustus verbouwd (generieke bevestiging in de gedeelde popup, plus taken met voortgang), dus dit is nu een actie toevoegen en geen mechanisme bouwen; Marcs vermoeden over de replica-logica klopt nog steeds, zie `project_manager.py` waar `replicas = 0 if (is_disabled or is_sleeping) else 1`. NOG ECHT OPEN, los van elkaar: PostgreSQL met schema's ontsluiten vanuit config en UI (de motor bestaat: `database_manager` heeft `create_schema`, `set_role_search_path` en een `db_schema`-parameter, en `DatabaseSecret` draagt `schema`); een bijlage vervangen, wat vandaag alleen als omweg kan omdat `validate_attachment_id` een id weigert dat al in de catalogus of staging zit, dus de echte vraag is "sta heruploaden onder hetzelfde id toe"; en een gestagede bijlage kunnen kiezen bij een nieuw component (stagen zelf bestaat al in `router_wizard_attachments.py`). TERUGPRATEN VOORDAT WE HET TOEZEGGEN: eigen healthchecks met contentcontrole kan Kubernetes niet met een `httpGet`-probe, die kijkt alleen naar de statuscode; contentcontrole vraagt een `exec`-probe of een sidecar, een andere constructie dan de drie velden die `HealthCheckConfig` nu heeft.
- [ ] BLIJFT STAAN, MAAR LOS VAN ONS PROJECT (7 augustus): bewaking op de VLAM-keten (vlam-wt8): er staat nu niets, waardoor onze proxy een dag lang naar een oud IP-adres van VLAM stuurde zonder dat iemand het zag. Een controle die periodiek een echte aanroep doet en het antwoord beoordeelt, niet alleen of de poort open is, want in dit geval antwoordde de verkeerde bestemming keurig met een geloofwaardige fout. Dit is een concreet geval van de bredere lacune uit `project_service_monitoring_gap`: geen zicht op dienstniveau.
- [ ] Account-link van de Keycloak-service zichtbaar maken in de UI (`features/futures/keycloak-account-link-in-ui.md`): de service heeft geen veld voor het koppelen van een SSO-login aan een vooraf aangemaakt account, terwijl `account-link` (automatic, confirm, verify) al volledig werkt in YAML en in de API. Alleen de UI-laag ontbreekt: options-provider, editable, visualizer en een regel in de layout. Let op de volgorde: `automatic` is pas verantwoord als het e-mailadres niet zelf te wijzigen is (identity-field-lock op de reconcile-weg) én de omzeiling hierboven dicht is.
- [ ] Certificaat eenvoudig te vertrouwen maken voor gebruikers (VLAM). Het hóe staat al in sectie 4 van `vlam-gebruikerstest.md`, met de root uit de verbinding halen plus drie env-vars voor curl, Node en Python. Wat ontbreekt: de geverifieerde root als bestand in de repo zodat niemand hem zelf hoeft te extraheren, en een Linux-variant, want daar liggen de paden anders en zullen mensen naar `update-ca-certificates` grijpen. Dat laatste is systeembreed vertrouwen en dat willen we juist niet. Wacht op bevestiging van de SHA-256-vingerafdruk door SSC-ICT of VLAM.
- [ ] E2E-modaaltesten stabiel maken: `tests/e2e/test_backup.py` en `test_edit_wizard.py` vallen willekeurig om zodra ze samen in één run zitten, los slagen ze altijd. De oorzaak is gevonden en het is geen productiefout: de klik op Opslaan levert helemaal géén verzoek op, want htmx gooit een tweede verzoek weg als hetzelfde element nog bezig is met de rerender van het selectveld. `networkidle` is daar geen goede wachtconditie voor, die is al waar voordat htmx klaar is met zijn eigen administratie. Uitgesloten met metingen: de opslag zelf werkt (tien geslaagde saves in dezelfde run), `validate_project_structure` kost 0,000s, en het formulier houdt na de swap gewoon zijn `hx-post` en `hx-ext`. Ik had in `tests/e2e/helpers/edit_modal.py` twee dingen aangepast, wachten op het antwoord in plaats van op netwerkstilte, plus een `_wait_htmx_idle` die wacht tot de klasse `htmx-request` weg is; dat bracht het van 1, 2, 3, 1 fouten naar 1, 1, 0 maar niet naar nul, en die wijziging staat inmiddels niet meer in de boom. Volgende stap: dezelfde wachtconditie ook op het openen van de modal en rekening houden met de blokken die de detailpagina sinds main lui nalaadt, want die vuren verzoeken af terwijl de test al klikt.
- [ ] Twee restpunten van de sleep-mode-fix hierboven. Ten eerste: eenmaal geschreven blijven die lege sleutels staan, want ze komen als bestaande data weer binnen en `remove_when_none` verwijdert alleen bij een lege inzending. Bestaande projectbestanden hebben dus een opruimactie nodig los van de formulierfix; `attachments: config: []` op een deployment-component in datzelfde bestand heeft dezelfde geur en hoort in dezelfde veeg. Wat GEEN openstaand punt meer is: de betekenis van een lege `match`. Besloten op 1 augustus dat die gewoon nergens op matcht, dus `enabled: true` zonder patronen is een geldige maar inerte toestand en `SleepModeConfig.matches()` blijft zoals hij is. Niet opnieuw ter discussie stellen.
- [ ] Vondst uit de schema-opruiming, pre-existing. (De eerste vondst — 22 bestanden die de rauwe schemavalidatie niet haalden — is opgelost door RC-32: de poort valideert nu tegen de gedeclareerde versie, en een afkeur is een ERROR die zegt dat het bestand niet verwerkt is.) `$defs/attachment-data-entry` en `$defs/service-config` waren al wees vóór al mijn wijzigingen. `service-config` is dode code. `attachment-data-entry` is dat NIET: het beschrijft de projectcatalogus van attachments (`id`, `filename`, AGE-versleutelde `content`) die vandaag door niets gevalideerd wordt, want de projectlaag delegeert naar de servicemodellen en `validate_service_configs` slaat de entry over omdat de catalogus onder `data` staat en niet onder `config`. Ik heb die def laten staan met een toelichting erin, want weghalen maakt het gat onzichtbaar. De nette oplossing past bij de rest: de catalogus in de attachments-service modelleren, wat vraagt of `data` onder `config` hoort of dat de projectwandeling `service_entry_config` moet gebruiken in plaats van `Project.service_config` (die twee geven nu verschillende antwoorden op dezelfde entry).
- [ ] Een test die een kernmodule herlaadt moet hem herstellen. Opgelost voor `tests/test_secret_key_failclosed.py` op 1 augustus (`importlib.reload(opi.core.config)` herbond `settings` naar een nieuw object terwijl elke module die `from opi.core.config import settings` deed het oude vasthield, waardoor een latere test het verkeerde object patchte en `test_sleep_mode_manifests` omviel in de volle run maar niet los). Openstaand: dit is een valkuil die zich herhaalt, dus overweeg een guard in `conftest.py` die aan het eind van een test controleert of `opi.core.config.settings` nog hetzelfde object is als bij aanvang, en luid faalt als een test dat stilletjes vervangt.
- [ ] REVIEWPOORT voor RC-13: controleer bij zijn PR of `instructions/services.md` beide helften draagt. RC-13 is op 1 augustus om 13:52 gedispatcht met zijn basis vastgepind op `073e104f`, en de merge die de ontbrekende 187 regels binnenhaalde (`99e8f722`) kwam om 15:59, dus te laat. Een poging het via `dclaude feedback RC-13` aan de draaiende sessie door te geven kwam niet aan ("Could not deliver message"); de taak is daardoor ook niet verstoord, status bleef `in_progress`. Taak 11 van dat plan breidt precies dat bestand uit, mogelijk aan de onvolledige versie. De toets is één grep: `grep -c hidden instructions/services.md` mag niet nul zijn, het bestand hoort 475 regels te tellen, en zowel "Forms and wizard screens" als "Hooks at a glance" moet erin staan. Klopt dat niet, dan is het een merge-conflict waarbij BEIDE helften erin moeten en kiezen fout is. Zelfde overweging geldt in mindere mate voor RC-12, die op dezelfde oude tip bouwt maar `services.md` niet aanraakt.
- [ ] `instructions/services.md` is op 28 juli door twee sessies tegelijk uitgebreid en dat geeft een conflict bij het samenvoegen naar main; beide helften moeten erin, kiezen is fout. Op `uniform-declarative-platform-services` (commit `94d397e7`) staat de sectie "Forms and wizard screens": registratie van een service levert nul UI op, wat `hidden=True` doet (de reden dat sleep-mode nergens in de wizard verschijnt terwijl de service volledig werkt), een tabel per `ConfigLayer` met wat automatisch gaat en wat je met de hand bedraadt, en het verschil tussen Editable en visualizer. Op `branches-samenvoegen-naar-main` (`84571ed7` en verder) staat de `detail_page_sections`-hook, een hooks-overzicht en de alinea over de drie plekken waar een project-level configsectie bedraad moet worden, plus de eis van een user-based sandbox-E2E. De merge-branch bevat het woord `hidden` nergens, dus juist de oorzaak van het sleep-mode-gat ontbreekt daar. Controle achteraf: `grep -c hidden instructions/services.md` op main mag niet nul zijn.
- [ ] Drie losse eindjes aan de PostgreSQL-scope uit RC-17, gemeten op 3 augustus. **Wat al goed is en niet aangeraakt hoeft:** compatibiliteit is dicht, want `PostgresqlDatabaseProjectConfig` heeft een validator die `scope: shared` invult als de sleutel ontbreekt, dus elk bestaand bestand valideert als shared en gedraagt zich als vandaag; de union kent exact twee leden dus iets anders faalt op validatie; en `extra="forbid"` op de shared-variant laat CNPG-velden als `storage` luid falen in plaats van ze te negeren. En `scope: project` is echt hetzelfde als de oude `namespace-postgresql-database`: `project_uses_dedicated_postgres()` is de enige poort, geeft voor allebei waar, en triggert `_create_infrastructure_namespace` plus `_create_infrastructure_resources`, dus dezelfde infrastructuurnamespace, hetzelfde CNPG-manifest in de deployments-repo en dezelfde ArgoCD-app; `ProjectScopeConfig` erft bovendien `DedicatedPostgresFields` uit `catalog/shared/postgres.py`, precies wat de namespaced service gebruikt. **Wat ontbreekt, punt 1:** `scope` staat helemaal niet in de UI, `editables.py` zegt expliciet dat het voorlopig alleen via YAML of API gaat, dus gebruikers kunnen nu niets kiezen. **Punt 2, en dat is het belangrijkste:** niets houdt een wijziging van shared naar project of terug tegen. De validatie accepteert het en de volgende verwerking richt gewoon een eigen cluster in of valt terug op de gedeelde, zonder één byte data te verplaatsen. Er moet een enforcer op die de vorige waarde vergelijkt en de wijziging weigert; het patroon staat er al met `UniqueSchemaEnforcer` uit dezelfde tak, en de vorige waarde is beschikbaar op het opslagpunt. **Punt 3:** bij verwijderen gaat een eigen database in één keer weg, want `delete_project_manager` doet `shutil.rmtree` op de infrastructuurmap en ArgoCD pruned het Cluster, waarna CNPG zijn PVC's meнeemt. De mark-for-deletion van PVC's kopiëren gaat hier niet één op één op: wij schrijven die PVC niet, CNPG maakt hem uit de Cluster-spec en is er eigenaar van, dus er is geen manifest om te hernoemen. Wat wij schrijven is het Cluster. Bij een PVC werkt de truc doordat `kustomization.yaml` uit de glob op schijf wordt opgebouwd en `*.marked-for-deletion.yaml` daar gewoon in blijft, dus de PVC blijft toegepast en leeft door; de markering is puur voornemen plus een rij in de `marked_for_deletion`-tabel. Doe je dat met het Cluster-manifest, dan blijft de hele database draaien: pods, geheugen, CPU, de infrastructuurnamespace, de secrets en de ArgoCD-app. Bij een PVC kost uitstel alleen schijfruimte, hier kost het een draaiende database waar niemand iets mee doet. Betere kandidaat is CNPG-hibernatie (annotatie `cnpg.io/hibernation`): pods uit, PVC's blijven, precies de bedoelde betekenis. Nog te controleren welke operatorversie wij draaien en of die het declaratief ondersteunt; het cluster was op 3 augustus niet bereikbaar om dat te meten. Derde weg is `reclaimPolicy: Retain` plus het Cluster echt weggooien, maar dan overleven de PV's als `Released` en wijst niets er meer naar, dus dat is de slechtste vindbaarheid. Het mechanisme is het probleem niet; het werk zit in het beleid, hoe lang, wie ruimt op, en waar zie je dat er een slapende database staat.
- [ ] `tests/integration/test_project_api.py::TestUpsertDeploymentEndpoint::test_upsert_deployment_empty_components` hangt af van de tests die voor hem draaien. Los slaagt hij in 13 seconden, in de volledige run valt hij om met `Project file not found: projects/test-project.yaml`. Gemeten op 12 augustus, na de RC-77-merge, en het is geen RC-77-regressie. De verdachte staat in de logregel ernaast: `ProjectStore warm working copy ready at /tmp/zad-projects-warm`, een vast pad buiten de tmp_path-fixtures, dus gedeeld tussen tests en tussen gelijktijdige runs. Voorstel: die warme werkkopie in tests naar een per-test map laten wijzen (de plek waar dat pad wordt bepaald is `opi/services/project_store.py` rond regel 259) in plaats van de test te repareren, want het pad zelf is het probleem en niet deze ene test. Open beslissing: of dat een fixture in `conftest.py` wordt die het pad omleidt, of een instelling die de ProjectStore zelf kent. Let op dat dit met de nieuwe "test alleen wat je wijzigt"-regel juist minder vaak zal opvallen, want de fout verschijnt alleen in een volledige run.
- [ ] Een time-out van de gebundelde ArgoCD-bevraging wordt niet gecacht, dus juist als ArgoCD ziek is helpt de cache niet. In `opi/services/argocd_overview.py` (RC-76, 12 augustus) keert de functie bij een time-out terug op regel 146, terwijl de cache pas op regel 164 gevuld wordt. Gevolg: zolang ArgoCD traag of onbereikbaar is, wacht ELKE weergave van de projectpagina opnieuw de volle `BEVRAGING_TIMEOUT_SECONDS` van 5 seconden, terwijl een geslaagde bevraging 15 seconden hergebruikt wordt. Dat is niet blokkerend, want 5 seconden met een tabel zonder statuskolom is beter dan de 30 seconden van de connector of een pagina die niet komt, en het bijt alleen in de storingssituatie. Voorstel: de mislukking ook opslaan, met een eigen kortere vervaltijd, zodat de storing hooguit eens per zoveel seconden gemeten wordt in plaats van bij elke render. Open beslissing: hoe kort die vervaltijd moet zijn, want te lang betekent dat de statuskolom leeg blijft nadat ArgoCD alweer terug is.
- [ ] `lotc-rvo` uit de dependencies halen (RC-97, 13 augustus). Het pakket staat in `pyproject.toml` en wordt geïnstalleerd, maar staat niet in `DESIGN_SYSTEMS` (`opi/core/templates_lotc.py` laadt `lotc-layout`, `nldd` en `lotc-forms`), dus het levert de applicatie geen stijl en geen componenten; geen enkele python-module importeert `lotc_rvo`. Waarom het toch niet zomaar weg kan: `tests/test_css_dode_variabelen.py` LEEST het tokenbestand van het pakket (`.venv/.../lotc_rvo/static/lotc/dist/@nl-rvo/design-tokens/index.css`) als de enige bron die kan zeggen of een `--rvo-*`-naam ooit echt heeft bestaan. Zonder het pakket verliest die poort zijn meetlat, en juist die poort houdt de shim in `static/css/lotc-app.css` eerlijk. Wat er nodig is om het WEL vast te kunnen stellen: (1) een besluit of die poort blijft - is het antwoord ja, dan moet de lijst met ooit-bestaande namen als eigen bestand in de repo, want dan is de dependency alleen nog een testfixture en dat is een dure manier om een lijst te bewaren; is het antwoord nee, dan vervalt de reden meteen; (2) meten of de kale bouwlijn nog sjablonen uit het pakket zou kunnen laden - vandaag staat `lotc_rvo/templates` niet in de zoekpaden van de Jinja-omgeving, maar dat is een gevolg van `DESIGN_SYSTEMS` en niet van het pakket. Zie `docs/opruiming-inventarisatie-rc97.md`, bak 2.
- [ ] `static/css/project-details.css` (24 kB) is geschreven voor een pagina die niet meer bestaat (RC-97, 13 augustus). Het bestand is NIET dood: `bg/project-tabs.html.j2` en `bg/_modals.html.j2` laden het allebei. Wat niet gemeten is, is hoeveel van de selectors nog ergens op slaan nu de map `opi/templates_lotc/project-details/` weg is. Niet weggooien op "deze klasse komt nergens meer voor in de sjablonen": een klasse kan de haak zijn waar JavaScript aan hangt (`deployment-section` en `.is-hidden` waren dat), en dan is er geen CSS-regel maar wel een gebruiker. Wat er nodig is om het vast te stellen: per selector drie plekken nalopen - de sjablonen, `static/js/*.js`, en de e2e-suite die op klassen selecteert - en pas weghalen wat op alle drie nul geeft. Zelfde afweging geldt voor `static/css/modal.css` en `static/css/wizard.css`, die op dezelfde manier door meerdere pagina's gedeeld worden. Zie `docs/opruiming-inventarisatie-rc97.md`, bak 2.
- [ ] De vernieuwing van het sandbox-wildcardcertificaat automatiseren, en de rest van tak `devex-updated` triëren (18 augustus). Het certificaat voor `*.sandbox.rijksapp.dev` liep vanochtend af en brak elke sandbox. De vernieuwflow bestond al sinds 20 mei (`scripts/renew-sandbox-cert.sh` plus de certbot-hooks die het `_acme-challenge`-TXT-record via de TransIP-API zetten), maar stond alleen op `devex-updated` en is nooit gemerged, dus op main was alleen het achterhaalde handmatige recept te vinden. Nu teruggezet met commit `b0062523`, nieuw certificaat geldig tot 16 november 2026. Wat open blijft: het is nog steeds een handmatige klus die iemand elke 90 dagen moet onthouden, en dat gaat een keer opnieuw mis. Voorstel: een CronJob in `rig-prd-operations` die lego draait, want de TransIP-sleutel is IP-gebonden aan productie en die namespace zit per definitie op dat IP, het secret `transip-credentials` hangt daar al, en pullen mag gewoon via `rcr.rijksapps.nl/dockerhub-rig/goacme/lego`. Openstaande beslissing: hoe het resultaat in deze repository komt, want versleutelen naar `.age` vraagt alleen de publieke recipient (`age1gpendphq3ar56pwkq886sd9tfhqfa4j4phc5h75sjnwpsde5qs5sq3ffum`) maar committen vraagt een schrijfweg vanuit het cluster naar GitHub, en die willen we misschien liever niet. Alternatief zonder die schrijfweg: de CronJob vernieuwt alleen in het cluster en een mens haalt het er eens per kwartaal uit, wat het probleem verplaatst maar niet oplost. Tweede punt bij dezelfde vondst: op `devex-updated` staan nog vijf andere ongemergede commits (projectsjablonen plus Forgejo-runner en CI/CD-pijplijn, het vooraf aanmaken van de `operations_manager`-database voor een verse sandbox, PVC- en presentatiefixes, een minio-versioning-doc met een keycloak-driftguard, en keycloak-verbindingsfixes). Die tak loopt 279 commits achter op main, dus dit is triëren en overzetten wat nog waarde heeft, niet mergen.
- [ ] Tabwisselen op de projectpagina: op het TESTPROJECT is er niets traags te meten, en drie verklaringen zijn onderweg gesneuveld. Ze staan hier alle drie, zodat ze niet terugkomen. Gemeten 19 en 20 augustus op een project met twee componenten en twee deployments. (1) "Het ontsleutelwerk met `age` op elk tabblad": uit code lezen afgeleid, niet gemeten, onjuist op deze schaal. Het serverantwoord is 44 tot 60 ms en voor alle negen tabbladen even duur. (2) "Elke tabwissel haalt 2927 KB opnieuw op": de response-events van Playwright vuren OOK bij een cache-treffer, dus status en content-length zeggen niets over de cache. Resource Timing zegt het wel: `transferSize` is 0 voor alle 2831 KB, met en zonder een eigen `Cache-Control` op `/static/lotc/*`. Die header is daarom teruggedraaid. (3) "Het duurt 607 ms": dat was `wait_for_load_state("networkidle")`, en die wacht per definitie 500 ms stilte af. De echte navigatie is 97 tot 117 ms totaal, opgesplitst in ongeveer 50 ms serverantwoord, 18 ms HTML parsen, 34 ms scripts plus het opbouwen van 179 web-componenten, en 5 ms daarna. Op het tabblad Componenten vuurt geen enkel blok zichzelf na het laden nog af. CONCLUSIE VOOR `hx-boost`: niet doen op deze cijfers. Het bespaart hooguit het HTML-parsen en een deel van het opbouwen, dus grofweg 30 van de 117 ms, en het serverantwoord blijft staan want htmx haalt dezelfde pagina op. Bovendien vervangt hx-boost standaard de hele `<body>`, dus die 179 componenten worden alsnog opnieuw opgebouwd tenzij je met `hx-select` op `#tab-<naam>` mikt; en de pagina leunt op plekken op een volledige herlaadbeurt (`closeEditModalAndReload` doet `window.location.reload()`). WAT WEL DE MOEITE VAN HET METEN WAARD IS, en waar de eerste verklaring alsnog kan kloppen: hoe dit schaalt. Het serverantwoord is op dit testproject 50 ms, maar het aantal `age`-procesforks groeit wel mee met het project (`1 + 1 + 2xrealms + deployments x componenten + componenten x 2 + helmcharts`), en op de tabbladen Overzicht, Metrics, Backups en Taken komen er blokken bij die zichzelf na het laden ophalen. Meet een echt project voordat je iets verandert. LET OP BIJ HET METEN, want op alle drie is deze analyse onderuitgegaan: een Playwright-SELECTOR kijkt door schaduwbomen heen en `page.evaluate` met `querySelector` niet; response-events zeggen niets over de cache (gebruik `performance.getEntriesByType('resource')` en kijk naar `transferSize`); en `networkidle` telt 500 ms stilte mee (gebruik de navigation-timing-fasen).
- [ ] Een mislukte opslag aan het EIND van de wizard gooit de hele sessie weg. Gemeld op 20 augustus: na alle stappen doorlopen te hebben kwam "Verwerking mislukt - Failed Git operations: configuratie van service 'invite' op projectniveau is ongeldig: Field required", en het projectbestand was niet opgeslagen, dus al het ingevulde werk was verdwenen. De DIRECTE oorzaak is gerepareerd (de wizard bewaarde de stapgegevens voordat de post_merge-hook de ontbrekende uitnodigingssleutel genereerde, zie `opi/web/router_wizard.py` rond regel 1038 en `tests/e2e/test_wizard_services_regressions.py::test_een_uitnodiging_zonder_eigen_sleutel_wordt_opgeslagen`), maar de VORM van de fout blijft: de modelvalidatie draait pas bij het schrijven, en tot dat moment kan de wizard een bestand samenstellen dat gegarandeerd wordt afgekeurd. Wat er dan gebeurt is het ergste van twee werelden, want de gebruiker krijgt een technische melding over een veld dat hij nooit heeft gezien EN is zijn werk kwijt. TWEE DINGEN OM TE OVERWEGEN, los van elkaar. (1) Valideer het samengestelde projectbestand tegen de servicemodellen op de REVIEW-stap, dus voordat er iets geschreven wordt; dan is een fout nog een formulierfout op een stap waar je naartoe kunt. (2) Laat een mislukte opslag de wizardstand niet weggooien, zodat je terug kunt naar de stap en het kunt herstellen; nu is de sessie na de fout leeg. OPEN BESLISSING bij (1): welke laag die validatie doet. De modellen zitten in de services, de wizard kent alleen editables, en de enige plek waar ze nu samenkomen is het opslagpunt. NOG EEN LOS PUNT dat bij dezelfde melding hoort: de foutmelding noemt het ontbrekende veld niet ("Field required. Geaccepteerde velden: default-language, active") terwijl het om `active[0].key` ging. De opsomming toont de velden van het BUITENSTE model, niet het pad naar het veld dat mist. Dat is voor elke servicevalidatie hetzelfde en is de moeite van het rechtzetten waard.
- [ ] `collectFormData` bestaat nergens. `static/js/wizard.js:96` en `:101` roepen hem aan in `_sequenceEditModal`, achter een `if (typeof collectFormData !== 'function') return;`. Die guard is altijd waar, want de functie is in de hele repo alleen nog beschreven (`features/codemirror-kv-editor.md:25`) en nergens gedefinieerd. Gevolg: het toevoegen of verwijderen van een item in een REEKS binnen de bewerkdialoog doet stil niets. Gevonden op 19 augustus bij ander werk in dat bestand, niet aangeraakt. Open beslissing: de functie terugbrengen (verzamelt de velden van `#edit-section-content` als JSON) of het pad weghalen als reeksen in die dialoog niet meer voorkomen - dat laatste eerst meten.
- [ ] De mailketen na de storing van 21 augustus 2026. De relay draait weer (de starthang zat in een spamfilter-download die Stalwart doet voordat zijn logger bestaat, met een hardgecodeerde timeout van 60 seconden; analyse in het blok `[spam-filter]` van `infrastructure/bootstrap/infrastructure/mail/controller/base/config.toml`), maar het verzenden is daarmee nog niet af. VIER DINGEN, uitgewerkt als punt 2 en 8 tot en met 10 in `plans/mail-vervolgpunten.md`. (1) NAAR BUITEN MAILEN KAN NIET: gemeten met twee testberichten vanaf `project-tvas-7pb` op productie gaf `robbert.uittenbroek@rijksoverheid.nl` een `250 ok: Message 139253981 accepted` en gaf een gmail-adres een `550 #5.1.0 Address rejected.` op de RCPT TO bij `rmrmail.rijksweb.nl`. Ons pad deed het goed, de upstream weigert externe ontvangers. Er is op 21 augustus contact geweest met het mailteam; DE UITKOMST VAN DAT GESPREK MOET HIER NOG IN, want die bepaalt of `features/send-email.md` bijgesteld moet worden (vandaag belooft dat document meer dan de keten kan) en of punt 3 hieronder nog urgenter wordt. (2) BOUNCES VERDWIJNEN STIL, en dat is het gevaarlijkste van de vier: mislukt een bezorging, dan adresseert Stalwart de DSN aan het envelope-adres `noreply-rijksapp+<project>@rijksoverheid.nl`, de upstream weigert dat adres OOK als ontvanger met `550`, en de relay noteert "discarding message after double bounce" en gooit hem weg. Een project hoort dus niets, wij ook niet, en de relaylog bewaart drie uur. Dit is de bounce-postbus die in `features/send-email.md` al als voorwaarde bij het aanzetten staat; hij is nu gemeten in plaats van voorspeld en blijkt ook een harde 550 op de eerste hop op te slokken. Voorstel: een echte postbus bij het mailteam die OPI via IMAP leegt, en zolang die er niet is minstens een alert op een mislukte bezorging. (3) DE MTA-STS-LOOKUP HANGT: voor elk ontvangend domein met een MTA-STS-record haalt Stalwart de policy over HTTPS op, en deze namespace heeft geen internet-egress, dus die fetch loopt in een timeout. Gemeten: de bezorgpoging naar gmail.com duurde 131 seconden, vrijwel helemaal in die lookup; bij `rijksoverheid.nl` viel het niet op omdat daar geen record staat. Voorstel: uitzetten, met dezelfde redenering als bij DKIM, want wij praten nooit rechtstreeks met de ontvangende server. (4) HET SPAMFILTER MOET AAN en staat uit omdat de regelset er niet is, niet omdat we hem niet willen; de weg erheen is `spam-filter.toml` VENDOREN in plaats van ophalen (76 KB, past ruim in een ConfigMap; versie 2.0.5 eist server 0.11.0 dus v0.11.8 voldoet), waarna nagelopen moet worden welke van de meegeleverde sleutels zelf naar buiten willen, want het bestand opent met `[asn] type = "resource"` met jsdelivr-URL's terwijl onze eigen config `type = "disable"` zet. LET OP DE KLASSE bij (3) en bij de starthang: een geblokkeerde uitgang levert hier geen foutmelding op maar een wachttijd, en op de sandbox is dat per definitie niet te reproduceren omdat Kind netwerkbeleid niet afdwingt. Elke meting aan dit onderdeel die alleen op de sandbox is gedaan, bewijst niets over ODCN.
- [ ] Een commit is geen wijziging in de bootstrap, en juist de noodrem landt daar. `bootstrap/rig-system/kustomize/overlays/odcn-production` wordt met de hand toegepast met `task bootstrap-argo-system` en niet door ArgoCD, dus een wijziging daarin doet niets tot iemand die taak draait, en niets laat zien dat dat nog moet. BEWEZEN op 21 augustus 2026: PR #168 zette de mailrelay uit "tot de RCA rond is", op beide schakelaars tegelijk, en die commit heeft het cluster nooit bereikt. De OPI-deployment draagt geen ArgoCD-tracking-id en had `MAIL_RELAY_API_URL` gewoon nog staan, dus OPI wees die hele periode naar de crashende relay terwijl iedereen dacht dat de dienst uit stond. Dat is erger dan gewone drift, want het gaat om het uitzetten van iets kapots: precies de wijziging die je onder druk maakt, landt in het deel van het platform waar een commit stil niets doet. Voorstel: een detectie in plaats van een afspraak, dus de gerenderde bootstrap vergelijken met de live toestand en het verschil melden, als CI-stap of als controle op de Services-statuspagina. Een ArgoCD-Application die de bootstrap zelf bewaakt kan ook, maar dat is een grotere ingreep en vraagt eerst een antwoord op de kip-en-ei met ArgoCD zelf. Open beslissing: welke van de twee, en waar de melding landt. Zie ook punt 11 in `plans/mail-vervolgpunten.md`.
- [ ] De limieten die de onbegrensde OOM auto-tune heeft achtergelaten. De escalatie zelf is gerepareerd (RC-157: een gedeelde pogingenteller die een geautomatiseerde refresh overleeft, een rem die live wordt gelezen, een grendel op de pod-template-hash, en een bovengrens van 8x de in de catalogus verklaarde limiet), maar die grens begrenst alleen NIEUWE groei. Wat de oude weg al had opgeblazen staat er nog. Een scan van productie op 24 augustus 2026 gaf deze verdachte waarden: `4039Mi rig-prd-mpfm-w3h/pr-137-clickhouse`, `3831Mi rig-prd-openp-4pw/poc-applicatie`, `3831Mi rig-prd-mpfb-8wh/pr-150-clickhouse`, `2163Mi rig-prd-regel-k4c/regelrecht-editor`, `1445Mi rig-prd-mpfm-w3h/pr-191-magazijnb`, `1445Mi rig-prd-mpfm-w3h/pr-189-magazijnb`, `1442Mi rig-prd-regel-k4c/upload-editor`, `1418Mi rig-prd-mpfpsm-lcl/pr-195-profiel`, `1165Mi rig-prd-algor-odc/deployment-1-component-1`. De niet-ronde getallen dragen de kenmerkende ladder van herhaalde vermenigvuldiging; ronde waarden zoals 1536Mi en 1Gi zijn vrijwel zeker met de hand gezet en horen er niet bij. WAAR HET ZIT: het zijn deployment-overrides in de projectbestanden in `zad-projects`, niet de catalogus-component; de verklaarde limiet daaronder is ongemoeid gebleven. INVENTARISEREN KAN AL: `PROJECTS=/pad/naar/zad-projects/projects task oom-growth-report` zet per project de huidige limiet naast de verklaarde, sorteert op verhouding en somt op wat buiten de 8x valt. Dat script leest alleen; het schrijft niets en commit niets. LET OP bij het beoordelen: `resources.history` wordt op vijf entries afgekapt (`_prune_resource_history`), dus de `oom-watcher`-regels in de geschiedenis zijn een ONDERGRENS - een ladder van negen stappen laat er hooguit vijf achter. De verhouding is de meting, de geschiedenis alleen een aanwijzing over de oorzaak. OPEN BESLISSING, en dat is de reden dat hier nog geen actie staat: mogen deze limieten automatisch worden teruggezet, of alleen gerapporteerd? Automatisch terugzetten raakt draaiende productieworkloads en kan een component die intussen ECHT zoveel nodig heeft de OOM in duwen; alleen rapporteren laat het beslag op de clusterquota staan tot iemand er per project naar kijkt. Wat er ook wordt gekozen: doe het per project en meet eerst het werkelijke gebruik, want de nachtelijke sweep verlaagt een opgeblazen limiet vanzelf zodra hij meetdata heeft - de bovengrens uit RC-157 kijkt bewust naar de RICHTING en blokkeert een verlaging niet.
- [ ] De mailaccounts van het platform declaratief maken in plaats van in code, MAAR pas nadat de relaymeting onderaan dit punt gedaan is. Vandaag staat elk platformaccount op drie plekken in code: tien constanten in `operations-manager/python/opi/core/config.py` (`MAIL_PLATFORM_*` en `MAIL_KEYCLOAK_*`), een eigen functie in `opi/manager/mail_manager.py` (`ensure_platform_account` naast `ensure_keycloak_account`), en een regel in de startvolgorde in `opi/core/startup.py`. Een derde afnemer betekent dus opnieuw drie keer code, terwijl de realms van Keycloak juist declaratief in `opi/configs/keycloak/*.yaml` staan; die inconsistentie is de aanleiding. Voorstel: een declaratie onder `opi/configs/mail/` met per account de naam, de weergavenaam, het dagbudget en waar het wachtwoord vandaan komt, plus één lus die ze verzorgt. Let op dat de twee bestaande accounts precies in dat laatste verschillen: `zad-platform` wordt door OPI zelf gegenereerd en bewaard, `zad-keycloak` komt uit het bootstrapgeheim `keycloak-mail-credentials` en OPI reconcilieert alleen. Kan het formaat dat verschil niet uitdrukken, dan liegt de declaratie over de helft van wat er gebeurt. LES DIE ERBIJ HOORT, en die is hier duur betaald: de keycloak-blauwdrukken declareerden maandenlang vier realmvelden die de handler helemaal niet las, en beloofden dus stil iets wat nooit gebeurde (gerepareerd in RC-159). Een nieuw declaratiebestand vraagt daarom meteen dezelfde toets die daar nu staat, een glob over de map die pint dat elke gedeclareerde sleutel ook echt gelezen wordt. OPEN BESLISSING, en dit is de voorwaarde: het is niet duidelijk of deze declaratie in ZAD thuishoort of in de relay zelf. Stalwart kan principals declaratief in configuratie dragen via een directory van het type `memory`, maar `[session.auth]` wijst EEN directory aan en de projectaccounts leven in de interne directory in de database. Of dat per LISTENER te scheiden valt, dus een tweede submission-listener voor platformcomponenten met een eigen `directory`, is NIET gemeten. Kan dat wel, dan staat de declaratie in de relayconfiguratie, verdwijnt de reconciliatiestap van OPI helemaal, en is een declaratie aan de ZAD-kant weggegooid werk. Doe die meting dus eerst. Niet urgent: het gaat vandaag om twee gevallen en het is een opschoning, geen nieuwe functionaliteit.
- [ ] Horen afgeronde plannen wel in git? `plans/` telt inmiddels 163 bestanden en ruim 20.000 regels, en groeit met elke taak. De eigen conventie in `instructions/README.md` omschrijft de map als "a finished plan for one piece of work, the contract for its PR", geschreven voor "whoever implements that plan". Dat publiek is per definitie leeg zodra de taak af is: van de vijf plannen die op 26 augustus 2026 untracked in de werkmap lagen hoorden er vier bij afgeronde taken (RC-148, RC-149, RC-150, RC-154) en een bij RC-151 die nog liep. DE KERN VAN DE TWIJFEL: het plan is een werkorder, geen oplevering, en hij staat AL ergens anders volledig bewaard, namelijk als taakomschrijving in de orchestrator (`orch info RC-157` geeft het hele plan terug). Git bewaart hem dus een tweede keer, zonder lezer. De rule of thumb twee alinea's lager in datzelfde README wijst dezelfde kant op: wat waar blijft nadat de code verschuift hoort in `features/` of `architecture/`, niet in een document dat aan een PR hangt. WAT AL GOED GAAT: de bouwers werken `features/` bij als onderdeel van de taak, dus de oplevering landt daar al. RC-157 en RC-160 raakten samen `features/auto-resource-tuning.md` en `features/oom-kill-watcher.md`; RC-159 deed hetzelfde voor `features/keycloak-mail.md` en `features/send-email.md`. Het opleverspoor bestaat dus, het werkorderspoor ernaast is de vraag. VOORSTEL, in twee stappen die los van elkaar kunnen: (1) leg in `instructions/README.md` vast dat een plan vergankelijk is, en dat een taak pas klaar is als het duurzame deel in `features/` staat, waarna het plan mag verdwijnen; dat maakt opruimen onderdeel van "klaar" in plaats van een aparte schoonmaakactie die niemand doet. (2) beslis wat er met de bestaande 163 gebeurt. OPEN BESLISSINGEN: horen ze weg (`git rm`, historie bewaart ze), naar `archive/` (die map bestaat al, met 13 bestanden), of buiten git via `.gitignore`? En als ze weg mogen: eerst per plan nalopen of de analyse erin ook echt in `features/` terecht is gekomen, want sommige plannen dragen een incidentreconstructie die nergens anders staat. LET OP bij die afweging: `plans/` buiten git zetten raakt de manier van werken, want `orch ship` leest een planbestand van schijf. Buiten git kan wel, maar dan moet duidelijk zijn waar zo'n bestand tijdens de uitvoering leeft.

- [ ] Redis staat nergens in beeld en zit in geen enkele backup. WAT HET IS: Redis is een gedeelde platformdienst die projecten afnemen, met `generate_redis_username(project, deployment)` en `generate_redis_key_prefix(project, deployment)` in `opi/utils/naming.py`, een `modal-edit-redis-config`-flow in het portaal, en een deployment die met `--appendonly` draait en dus naar schijf persisteert. Er zit projectdata in, gescheiden op sleutelprefix, en het is geen pure cache. GAT 1, ZICHT: er is geen redis-exporter. Gemeten 28-08-2026: Mimir prd heeft zeven reeksen `argocd_redis_request_*`, en dat zijn ArgoCD's clientmetrieken over zijn eigen Redis, niet onze toestand; onze eigen Prometheus heeft nul reeksen met "redis" erin. Geen geheugengebruik, geen keys, geen evictions, geen persistence-status. VOORSTEL: een redis-exporter als sidecar op 9121 in `infrastructure/bootstrap/infrastructure/redis/controller/base/deployment.yaml`, met `prometheus.io/scrape`, `prometheus.io/port` en `prometheus.io/path` erop. Een aparte scrape-job is waarschijnlijk niet nodig: de job `kubernetes-pods` in onze eigen Prometheus doet clusterbrede annotatie-discovery en pikt langs die weg al keycloak, rig-db en de operations-manager op. GAT 2, BACKUP: er is geen enkel backuppad voor Redis. De PVC `rig-redis-data` draagt wel `backup.rig.nl/enabled: "true"`, maar dat label doet niets zolang er geen project met een deployment aan hangt, want backups draaien per (project, deployment) uit een projectbestand en `rig-prd-operations` is geen project. Anders dan bij Postgres (`pg_dump` per database) en MinIO (mirror per bucket) bestaat er ook geen "dump de keys van dit prefix". OPEN BESLISSINGEN: positioneren we Redis als cache, en zeggen we dan expliciet tegen projecten dat wat erin staat weg kan zijn, of als opslag, en moet er dan een dump per project bij? De keuze voor de exporter zelf ligt bij het platformteam.

- [ ] Losse eindjes van de verhuizing van de backupopslag naar buiten het cluster. AANLEIDING: het gesprek over NetApp of Ceph; achtergrond staat in `docs/backup-architectuur-overzicht.md`. DRIE CODEFIXES die nodig zijn zodra de bestemming extern is, alle drie klein, geen ervan nu gedaan omdat de vorm van hun beleid bepaalt hoe ze eruit moeten zien: (1) `configure_alias` in `opi/connectors/minio_mc.py:279` test de verbinding met `mc ls <alias>`, dus ListAllMyBuckets op de root, en faalt de hele backup als het account alleen bij één bucket mag; test op `alias/bucket`. (2) `_ensure_bucket_exists` doet elke run een `mc mb`; "already exists" wordt getolereerd maar een Access Denied zonder CreateBucket-recht gooit, dus in single-bucketmodus alleen bestaan controleren. (3) de mc-alias aan OPI-kant staat hardgecodeerd op `secure=False` in `manager/backup/base.py`, terwijl de backup pods `BACKUP_S3_USE_TLS` wel respecteren. LET OP bij de aanvraag: endpoint is `host:port` zonder schema en zonder dubbele punt plakt de code er `:9000` achter; vraag om geen lifecycle rules of eigen retentie op onze bucket, want Kopia beheert dat zelf en zijn blobs zijn content-geadresseerd, dus een regel als "verwijder objecten ouder dan 90 dagen" sloopt de repository stil. GEMETEN 28-08-2026: alle backups samen 1,8 GiB op een PVC van 98 GiB, alle projectbuckets samen 4,1 GiB, dus capaciteit is het probleem niet en de vraag is puur een eigen faaldomein. NOG OPEN: de staging-limiet van 5Gi in `manifests/backup-bucket-pod.yaml.jinja` is gezet op die 4,1 GiB en moet herijkt worden zodra de MinIO-metrics binnenkomen; de versioned bucket van de docs-applicatie zit niet in de backupscope omdat die deployment via helmfile draait en geen `minio-storage`-service declareert (`task_handlers_backup.py:162`), en daar hangt de vraag aan of de versiegeschiedenis mee moet, want `mc mirror` pakt alleen de huidige versies; `task_handlers_backup.py:178` neemt met `minio_components[0]` altijd maar één bucket per deployment, een tweede verdwijnt geruisloos; en `features/storage-metrics-monitoring.md` beschrijft `minio_bucket_usage_total_bytes` alsof het werkt terwijl de scrape-job tot 28-08-2026 op 403 stond.
