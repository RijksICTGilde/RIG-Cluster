apiVersion: v1
kind: Secret
metadata:
  name: argocd-admin-credentials
  namespace: rig-system
type: Opaque
stringData:
  # The admin password for ArgoCD (bcrypt hash)
  admin.password: "PLACEHOLDER_BCRYPT_HASH" # @secret-gen:bcrypt:16,path=bootstrap/rig-system/kustomize/overlays/{{.BOOTSTRAP_CLUSTER_FOLDER}}