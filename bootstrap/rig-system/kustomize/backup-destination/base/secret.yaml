# This is a template for local development
# For production, this would be SOPS encrypted
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: rig-backup-destination
  labels:
    app.kubernetes.io/name: minio
    app.kubernetes.io/component: backup-storage
type: Opaque
stringData:
  root-user: "backup-admin"
  root-password: "backup-secret-key-local"
