apiVersion: v1
kind: Service
metadata:
  name: operations-manager
  namespace: rig-system
  labels:
    app: operations-manager
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: operations-manager