apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generators:
  - decrypt-sops.yaml

resources:
  - ../../base
  - cluster-binding.yaml
  - cluster-role.yaml
  - ingress.yaml
  - configmap.yaml
  - pvc.yaml

patches:
  - path: patches/deployment.yaml
    target:
      kind: Deployment
      name: operations-manager
  # Enable hot-reload mode for development (default)
  - path: patches/enable-hot-reload.yaml
    target:
      kind: Deployment
      name: operations-manager
