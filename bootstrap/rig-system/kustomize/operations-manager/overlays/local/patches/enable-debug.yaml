apiVersion: apps/v1
kind: Deployment
metadata:
  name: operations-manager
spec:
  template:
    spec:
      containers:
      - name: operations-manager
        env:
        - name: DEBUG_MODE
          value: "debug"
        ports:
        - containerPort: 5678
          name: debug
          protocol: TCP
