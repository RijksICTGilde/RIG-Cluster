apiVersion: apps/v1
kind: Deployment
metadata:
  name: operations-manager
spec:
  template:
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: operations-manager
        image: operations-manager:latest
        imagePullPolicy: Never
        env:
        - name: TMPDIR
          value: "/tmp"
        - name: TEMP
          value: "/tmp"
        - name: TMP
          value: "/tmp"
        - name: PYTHONTMPDIR
          value: "/tmp"
        - name: ENVIRONMENT
          value: "local"
        - name: LOCAL_DEVELOPMENT
          value: "true"
        - name: REQUESTS_CA_BUNDLE
          value: "/etc/ssl/certs/kind-local-ca.crt"
        - name: SSL_CERT_FILE
          value: "/etc/ssl/certs/kind-local-ca.crt"
        volumeMounts:
        - name: kube-api-access
          mountPath: /var/run/secrets/kubernetes.io/serviceaccount
          readOnly: true
        - name: tmp
          mountPath: /tmp
        - name: tmp
          mountPath: /var/tmp
        - name: config-volume
          mountPath: /etc/config
          readOnly: true
        - name: ca-certificate
          mountPath: /etc/ssl/certs/kind-local-ca.crt
          readOnly: true
        resources:
          limits:
            cpu: "4"
            memory: 512Mi
          requests:
            cpu: "100m"
            memory: 256Mi
      volumes:
      - name: kube-api-access
        projected:
          sources:
          - serviceAccountToken:
              path: token
          - configMap:
              name: kube-root-ca.crt
              items:
              - key: ca.crt
                path: ca.crt
          - downwardAPI:
              items:
              - path: namespace
                fieldRef:
                  fieldPath: metadata.namespace
      - name: tmp
        emptyDir:
          sizeLimit: 2Gi
      - name: config-volume
        configMap:
          name: operations-manager-config
      - name: ca-certificate
        hostPath:
          path: /etc/ssl/certs/kind-local-ca.crt
          type: File
      serviceAccountName: namespace-manager
