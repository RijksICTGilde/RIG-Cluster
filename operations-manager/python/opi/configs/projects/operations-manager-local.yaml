name: operations-manager
display-name: Operations Manager
description: RIG Platform Operations Manager

services:
  - keycloak:
      config:
        template: operations-manager-realm
        users:
          - username: admin
            email: admin@sandbox.rijksapp.dev
            firstName: Admin
            lastName: User
            enabled: true
            credentials:
              - type: password
                value: admin1234
