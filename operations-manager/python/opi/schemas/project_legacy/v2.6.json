{
  "$comment": "v2.6 -> v2.7 (RC-60) moved the seven web-address settings from the deployment root to the publish-on-web service config on that deployment. Until v2.7 a file carried them at the deployment root, so the schema of v2.6 re-adds them here. Deployment service config is an open envelope, so a file that already carries them under the service validates at both versions."
}
