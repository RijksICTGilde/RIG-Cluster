{
  "$defs": {
    "SendEmailAccount": {
      "additionalProperties": true,
      "description": "One SMTP account on the relay, as OPI created it (one per cluster).\n\nShaped like ``KeycloakRealm``: matched on ``cluster``, written by the platform, and\nthe only place this account's password lives. ``extra=\"allow\"`` so a field added later\ndoes not make existing project files unreadable.",
      "properties": {
        "bounce-address": {
          "description": "Address bounces for this account return to; carries the project name so a bounce is traceable.",
          "title": "Bounce-Address",
          "type": "string"
        },
        "cluster": {
          "description": "Cluster this account was created for. Written by the platform.",
          "title": "Cluster",
          "type": "string"
        },
        "from-address": {
          "description": "Address the relay pins the envelope sender and the From: header to for this account.",
          "title": "From-Address",
          "type": "string"
        },
        "password": {
          "description": "That account's password, AGE-encrypted or 'plain:'-prefixed.",
          "title": "Password",
          "type": "string"
        },
        "username": {
          "description": "SASL username the application authenticates to the relay with.",
          "title": "Username",
          "type": "string"
        }
      },
      "required": [
        "cluster",
        "username",
        "password",
        "from-address",
        "bounce-address"
      ],
      "title": "SendEmailAccount",
      "type": "object"
    },
    "SendEmailApproval": {
      "additionalProperties": true,
      "description": "Whether an administrator has allowed this project to send mail (aanvulling 6).\n\nShaped like the domain/subdomain entries publish-on-web stores: a status plus the\nverdict history, so the file is the audit trail. Written only through the generic\napproval flow (``opi/services/approvals.py``).",
      "properties": {
        "history": {
          "description": "Verdict history: one {date, status, by, message} entry per decision.",
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "title": "History",
          "type": "array"
        },
        "status": {
          "description": "requested, approved or denied. Written by the platform's approval flow.",
          "title": "Status",
          "type": "string"
        }
      },
      "required": [
        "status"
      ],
      "title": "SendEmailApproval",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "What a project may decide about its outgoing mail.",
  "properties": {
    "accounts": {
      "description": "Per-cluster SMTP accounts on the relay. Written and managed by the platform: carried over on a write, so a caller neither has to send it nor can lose it by leaving it out.",
      "items": {
        "$ref": "#/$defs/SendEmailAccount"
      },
      "title": "Accounts",
      "type": "array",
      "x-platform-managed": true
    },
    "approval": {
      "anyOf": [
        {
          "$ref": "#/$defs/SendEmailApproval"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Whether an administrator has allowed this project to send mail. Written by the platform: until it says approved, no account is created, no network policy is written and no SMTP credentials reach the deployment.",
      "x-platform-managed": true
    },
    "from-domain": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Sender domain, when the project has one of its own. Left out for the platform mail domain. Written by the platform: a domain of your own needs a DKIM record in its zone first AND a rule for that domain in the relay's identity script (which today only allows the platform mail domain), and that path is set up by hand until a project asks for it, so this is not self-service.",
      "title": "From-Domain",
      "x-platform-managed": true
    },
    "from-local-part": {
      "anyOf": [
        {
          "pattern": "^[a-z0-9]([a-z0-9._-]{0,62}[a-z0-9])?$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "First half of the local part of the sender address, e.g. 'noreply'. Defaults to 'noreply'. The project name is appended to it, so the address becomes noreply.<project>@<maildomein>: the relay pins the From: header to an address carrying the account name, and an address may only exist once on the whole relay.",
      "title": "From-Local-Part"
    },
    "from-name": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Display name shown to the recipient, e.g. 'Algoritmeregister'. The address itself is fixed.",
      "title": "From-Name"
    },
    "messages-per-day": {
      "anyOf": [
        {
          "maximum": 5000,
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Messages this project may send per day, at most 5000. Leave it out for the platform default. This is the agreed budget, recorded on the account: the relay itself enforces one ceiling of 5000 per account per day, because an account on the relay carries no limit of its own.",
      "title": "Messages-Per-Day"
    }
  },
  "title": "SendEmailConfig",
  "type": "object"
}
