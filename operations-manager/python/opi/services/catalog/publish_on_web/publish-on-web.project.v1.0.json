{
  "$defs": {
    "AllowedDomainEntry": {
      "additionalProperties": false,
      "properties": {
        "domain": {
          "description": "The domain this project may publish on.",
          "title": "Domain",
          "type": "string"
        },
        "history": {
          "default": [],
          "description": "Every decision made about it, oldest first.",
          "items": {
            "$ref": "#/$defs/DomainHistoryEntry"
          },
          "title": "History",
          "type": "array"
        },
        "issuer": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Certificate issuer to use for this domain; the platform default when absent.",
          "title": "Issuer"
        },
        "restricted-subdomains": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whether every subdomain under it needs its own approval.",
          "title": "Restricted-Subdomains"
        },
        "status": {
          "description": "Approval state of the domain itself.",
          "enum": [
            "requested",
            "approved",
            "denied"
          ],
          "title": "Status",
          "type": "string"
        },
        "supports-dots": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whether a dotted subdomain is allowed under it; a wildcard certificate covers one level only.",
          "title": "Supports-Dots"
        }
      },
      "required": [
        "domain",
        "status"
      ],
      "title": "AllowedDomainEntry",
      "type": "object"
    },
    "AllowedSubdomainDetail": {
      "additionalProperties": false,
      "properties": {
        "history": {
          "default": [],
          "description": "Every decision made about it, oldest first.",
          "items": {
            "$ref": "#/$defs/DomainHistoryEntry"
          },
          "title": "History",
          "type": "array"
        },
        "name": {
          "description": "The subdomain, without the domain it sits under.",
          "title": "Name",
          "type": "string"
        },
        "status": {
          "description": "Approval state of this subdomain.",
          "enum": [
            "requested",
            "approved",
            "denied"
          ],
          "title": "Status",
          "type": "string"
        }
      },
      "required": [
        "name",
        "status"
      ],
      "title": "AllowedSubdomainDetail",
      "type": "object"
    },
    "AllowedSubdomainEntry": {
      "additionalProperties": false,
      "properties": {
        "domain": {
          "description": "The domain these subdomains sit under.",
          "title": "Domain",
          "type": "string"
        },
        "subdomains": {
          "default": [],
          "description": "The subdomains requested under it, with their approval state.",
          "items": {
            "$ref": "#/$defs/AllowedSubdomainDetail"
          },
          "title": "Subdomains",
          "type": "array"
        }
      },
      "required": [
        "domain"
      ],
      "title": "AllowedSubdomainEntry",
      "type": "object"
    },
    "DomainHistoryEntry": {
      "additionalProperties": false,
      "properties": {
        "by": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Who decided it.",
          "title": "By"
        },
        "date": {
          "description": "When this decision was recorded, ISO 8601.",
          "title": "Date",
          "type": "string"
        },
        "message": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The reason given with the decision.",
          "title": "Message"
        },
        "status": {
          "description": "What was decided: requested, approved or denied.",
          "enum": [
            "requested",
            "approved",
            "denied"
          ],
          "title": "Status",
          "type": "string"
        }
      },
      "required": [
        "date",
        "status"
      ],
      "title": "DomainHistoryEntry",
      "type": "object"
    },
    "DomainsConfig": {
      "additionalProperties": false,
      "description": "The project-wide domain approval block.",
      "properties": {
        "allowed-domains": {
          "description": "The domains this project may publish on, with their approval state.",
          "items": {
            "$ref": "#/$defs/AllowedDomainEntry"
          },
          "title": "Allowed-Domains",
          "type": "array"
        },
        "allowed-subdomains": {
          "description": "Per domain, the subdomains this project requested and their approval state.",
          "items": {
            "$ref": "#/$defs/AllowedSubdomainEntry"
          },
          "title": "Allowed-Subdomains",
          "type": "array"
        }
      },
      "title": "DomainsConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Project layer: which domains this project may publish on, plus the TLS default.\n\nTwo things, because the code already holds both there. ``domains`` is the approval\nblock the v2.5 migration relocated here. ``tls`` / ``attachment`` are inherited rather\nthan repeated: level 3 of the cascade in\n``project_file_handler._resolve_publish_on_web_config`` is the project-level entry, so\na project can set one certificate mode for every component that does not override it.\nDropping them here would have made every such project file fail validation.",
  "properties": {
    "attachment": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Id of the attachment holding the certificate PEM. Required when tls is 'provided'.",
      "title": "Attachment"
    },
    "domains": {
      "anyOf": [
        {
          "$ref": "#/$defs/DomainsConfig"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Project-level domain approvals. Written by the platform's approval flow and carried over on a write, so a caller neither has to send it nor can lose it by leaving it out.",
      "x-platform-managed": true
    },
    "tls": {
      "anyOf": [
        {
          "enum": [
            "standard",
            "passthrough",
            "provided"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "How TLS is terminated: 'standard' uses the platform certificate, 'passthrough' lets the pod present its own, 'provided' terminates a certificate you supply as an attachment. Absent means inherit (deployment > component > root > standard).",
      "title": "Tls"
    }
  },
  "title": "PublishOnWebProjectConfig",
  "type": "object"
}
