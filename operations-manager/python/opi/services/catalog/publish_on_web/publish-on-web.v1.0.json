{
  "additionalProperties": false,
  "description": "Component layer: this component uses the service, and how TLS is terminated.",
  "properties": {
    "attachment": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Id of the attachment holding the certificate PEM. Required when tls is 'provided'.",
      "title": "Attachment"
    },
    "tls": {
      "anyOf": [
        {
          "enum": [
            "standard",
            "passthrough",
            "provided"
          ],
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "How TLS is terminated: 'standard' uses the platform certificate, 'passthrough' lets the pod present its own, 'provided' terminates a certificate you supply as an attachment. Absent means inherit (deployment > component > root > standard).",
      "title": "Tls"
    }
  },
  "title": "PublishOnWebComponentConfig",
  "type": "object"
}
