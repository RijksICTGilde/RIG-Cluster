{
  "additionalProperties": false,
  "properties": {
    "path": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Path metrics are scraped from; /metrics when left out.",
      "title": "Path"
    },
    "port": {
      "anyOf": [
        {
          "maximum": 65535,
          "minimum": 1024,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Port metrics are scraped from; the application port when left out. Must be 1024 or higher: images run non-root and cannot bind below that.",
      "title": "Port"
    }
  },
  "title": "MetricsScraperConfig",
  "type": "object"
}
