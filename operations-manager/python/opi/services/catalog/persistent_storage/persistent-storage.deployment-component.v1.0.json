{
  "$defs": {
    "Revision": {
      "additionalProperties": false,
      "description": "One generation's resource, with the actions that produced and changed it.",
      "properties": {
        "actions": {
          "default": [],
          "description": "Audit trail of what was done to this generation.",
          "items": {
            "$ref": "#/$defs/RevisionAction"
          },
          "title": "Actions",
          "type": "array"
        },
        "created_at": {
          "description": "When this generation was provisioned, ISO 8601.",
          "title": "Created At",
          "type": "string"
        },
        "generation": {
          "description": "The generation number this revision records.",
          "title": "Generation",
          "type": "integer"
        },
        "resource": {
          "description": "The provisioned resource this generation points at (database name, bucket name).",
          "title": "Resource",
          "type": "string"
        },
        "status": {
          "description": "'active' for the generation in use, 'superseded' once a newer one took over.",
          "title": "Status",
          "type": "string"
        },
        "superseded_at": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When it was retired; absent on the active generation.",
          "title": "Superseded At"
        }
      },
      "required": [
        "generation",
        "resource",
        "status",
        "created_at"
      ],
      "title": "Revision",
      "type": "object"
    },
    "RevisionAction": {
      "additionalProperties": false,
      "description": "One entry in a revision's audit trail (created, cloned, restored, ...).",
      "properties": {
        "source": {
          "description": "Where it came from: the deployment, project or external source cloned from.",
          "title": "Source",
          "type": "string"
        },
        "timestamp": {
          "description": "When it happened, ISO 8601.",
          "title": "Timestamp",
          "type": "string"
        },
        "type": {
          "description": "What happened: created, cloned, restored, ...",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "type",
        "source",
        "timestamp"
      ],
      "title": "RevisionAction",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Per-mount clone state, carried on the deployment-component layer.\n\nA deployment records which PVC generation each mount points at, as a list of\n``{reference, config}`` items keyed by the mount name. The content is the shared clone\nstate, so this only exists to give the storage services something to return from\n``config_model_for(DEPLOYMENT_COMPONENT)``; their ``config_model`` describes the mount\nspecs on the component layer and cannot describe both.",
  "properties": {
    "generation": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Current generation, incremented on each clone. Written by the platform.",
      "title": "Generation"
    },
    "revisions": {
      "default": [],
      "description": "Every generation this resource has had, newest last. Written by the platform.",
      "items": {
        "$ref": "#/$defs/Revision"
      },
      "title": "Revisions",
      "type": "array"
    }
  },
  "title": "StorageCloneState",
  "type": "object"
}
