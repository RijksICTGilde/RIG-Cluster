"""resource-tuning service package.

Holds the service's owned config (``config.py`` / ``config_model.py``). The ``Service``
subclass and its after-sync observation hook are added here when the service is
registered.
"""
