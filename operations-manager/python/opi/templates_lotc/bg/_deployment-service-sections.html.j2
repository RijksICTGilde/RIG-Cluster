{#
  De blokken die de DIENSTEN per deployment leveren.

  Hier stond een TABEL die per dienstsjabloon zijn tegenhanger aanwees, met als
  verantwoording dat die sjablonen bij hun dienst liggen en die map niet op het zoekpad van
  deze omgeving stond. Die verantwoording klopte niet: opi/core/templates_lotc.py hangt
  CATALOG_DIR achteraan op de searchpath.

  Waarom de tabel weg MOEST en niet alleen mocht: het was handwerk op de verkeerde plek.
  Een dienst draagt alles wat hij is in zijn eigen map (RC-36), dus ook zijn sjabloon. Met
  een tabel hier moest een nieuwe dienst een bestand BUITEN zijn eigen map bijwerken, en
  vergat hij dat, dan viel zijn blok stilzwijgend terug op de oude weergave - kale,
  onopgemaakte HTML midden op de pagina.

  Daarna stond hier lotc_counterpart(), dat een blok OVERSLOEG als er geen
  ``-lotc``-versie naast het roos-sjabloon lag. Ook dat was er zolang er twee bouwlijnen
  waren. Elke dienst heeft nu nog EEN sjabloon, onder zijn eigen naam, en dat wordt gewoon
  gerenderd; dat het er ook echt is toetst tests/test_lotc_dienstblokken.py.
#}
{% for section in sections %}
    {% include section.template %}
{% endfor %}
