{#
  Keuzelijst voor de LOTC-bouwlijn.

  Grootste verschil met de roos-versie: die gaf de opties mee als JSON in een prop
  (:options="{{ options_json }}"). In LOTC zijn opties KINDEREN. Daardoor kan dit
  template rechtstreeks over field.options lopen en is de JSON-omweg overbodig -
  options_json wordt hier niet meer gebruikt.
#}
{% from "widgets/_macros.html.j2" import combined_helper, description_text, render_examples %}
{{ description_text(field) }}
<c-select-field
    id="{{ field.path|e }}"
    name="{{ field.path|e }}"
    label="{{ field.label|e }}"
    :help="combined_helper(field)|trim or none"
    :required="field.required"
    :disabled="field.readonly"
    :value="value_str or none"
    :error="(field.errors[0] if field.errors else none)"
    :attrs="field_attrs(field)"
>
    {# Bewust kale <option>-elementen en geen <c-option>. c-option rendert onder NLDD
       een <nldd-menu-item>, en c-select-field zet zijn kinderen in een native <select>.
       Een browser gooit alles weg wat binnen een <select> geen <option> is, dus de lijst
       kwam leeg te staan - zichtbaar op een screenshot, onzichtbaar in de HTML. Gemeld
       bij het LOTC-project. #}
    {% for option in field.options or [] %}
        <option value="{{ option.value|e }}"{% if option.value|string == value_str %} selected{% endif %}>{{ option.label|e }}</option>
    {% endfor %}
</c-select-field>{{ render_examples(field) }}
