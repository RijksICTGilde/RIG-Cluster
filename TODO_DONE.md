# TODO_DONE

Afgerond werk, tijdelijk bewaard zodat het nagelopen kan worden.
De meting en de reden staan erbij, want dat is wat vandaag vier keer voorkwam dat we
iets bouwden dat al af was.

- [x] RC-30 AF EN GEMERGED (6 augustus): taken tonen nu de stappen die ze zetten. De stappen worden gerapporteerd in `project_manager.py`, waar het werk gebeurt, en niet in de handler die de taak start; dat is dieper dan het plan voorschreef en beter. De eis dat een stap niet mag liegen is getest (`test_noop_says_so_instead_of_ticking_off_work_it_skipped`). Zie `features/task-steps.md`.
- [x] RC-29 AF EN GEMERGED (6 augustus): verwijderen en herverwerken lopen nu langs dezelfde bevestiging als een service-actie; `showDangerConfirmation` en zijn hardgecodeerde titelmap zijn weg. BELANGRIJK, uit hun eigen securityreview: het voortgangsfragment werd gerenderd en daarna nog eens door `process_components` gehaald, dat gerenderde HTML opnieuw als Jinja parst. Autoescape dekt `< > & " '` maar niet `{{`, dus tekst uit de URL (bijlage-id, componentnaam, deploymentnaam) werd uitgevoerd in de OPI-pod, bereikbaar voor iedereen die een actie mag bevestigen op zijn eigen project. Beide helften gedicht: nog een keer renderen, en die namen worden nu tegen het project gecontroleerd. Zie `tests/test_progress_fragment_injection.py`.
- [x] RC-16 AF EN GEMERGED (5 augustus geverifieerd): Keycloak service-account voor OPI plus OTP op de realm-admins. Op de sandbox live bevestigd: OPI draait op client-credentials en het tweede master-account `admin-otp` wordt bij bootstrap met een OTP-seed aangemaakt. WAT ER NOG MOET GEBEUREN, en dat is uitrol en geen code: de service-account op productie inrichten, en pas daarna handmatig OTP aanzetten op het gedeelde `admin`-account (of het uitzetten), want anders sluit je jezelf buiten. Zie ook `features/futures/keycloak-rechten-overdragen.md` voor het model dat we eigenlijk wilden en niet hebben.
- [x] RC-17 AF EN GEMERGED (5 augustus geverifieerd): meerdere PostgreSQL-schema's en een scope-keuze op de service. De drie losse eindjes die eruit voortkwamen staan apart op deze lijst (scope-enforcer, hibernation, compatibiliteit).
- [x] GROTENDEELS AF (6 augustus gemeten, de oude tekst klopte niet meer): de `Available != True`-guard die het OOM-pad afkapte heeft nu een `oom_triggered`-uitzondering met de reden erbij, en `resource-tuning` IS een systeemdienst die op `AFTER_SYNC` haakt. Wat nog nagekeken moet worden is het tweede punt uit de oorspronkelijke melding: schrijft de nachtelijke sweep nog het gedeelde root-limiet per deployment? En `asses-k2n/pr-450` moet gecontroleerd worden of die er inmiddels uit is.
- [x] Sleep-mode schreef lege formuliervelden als lege waarden het projectbestand in. GEFIXT 1 augustus (niet uitgerold). Waargenomen op `dimp-r0v`: `match: []`, `waker-component: null`, `title: null` en `description: ''`. Oorzaak, en het is wel degelijk de gewone editables-weg zoals verwacht: een leeg tekstveld komt binnen als `""` en niet als `None` (dat laatste slaat `_write_field` over op `processor.py:366`), de converter maakt daar `None` of `[]` van, en zonder `remove_when_none` valt dat in de `else`-tak die de lege waarde wegschrijft; `description` had helemaal geen converter en schreef de lege string letterlijk weg. Fix: `remove_when_none=True` op `match`, `waker-component`, `title` en `description` in `catalog/sleep_mode/editables.py`, plus een `EmptyToNoneConverter` op `description`. Sleep-mode was de enige die die vlag nul keer gebruikte terwijl hij elders 27 keer staat. Bewust niet op `enabled` en `waker`: dat zijn booleans, de vlag behandelt `False` als leeg, en `waker` heeft default `True` dus uitvinken zou hem stil terugzetten op aan. Regressietest `TestSleepModeEmptyFieldsLeaveNoKeys` in `tests/test_editable_processor.py`, geverifieerd dat hij op de oude code faalt met precies het waargenomen symptoom. Let op bij het schrijven van zulke tests: `process_json_submission` leest GENESTE JSON via padtraversal, geen platte sleutels, en respecteert `virtualize`, dus de ingezonden structuur moet `_services-config/<service>/config/<veld>` genest zijn.
- [x] Kloonstatus (`generation` + `revisions`) als herbruikbaar patroon. GEBOUWD 1 augustus (niet uitgerold): `catalog/shared/revisions.py` met `RevisionAction`, `Revision` en een `CloneState`-mixin, in de geest van `shared/storage.py`. `postgresql-database` en `minio-storage` hebben nu een `config_model.py` dat die mixin componeert (minio ook `enable-versioning`), plus gegenereerde schemafragmenten. Verplichte velden zijn gebaseerd op wat 33 echte revisies in de productiebestanden dragen: alles behalve `superseded_at`. Geverifieerd met een audit over alle 47 productiebestanden na `migrate_to_latest()` in memory: postgresql-database 31 keer OK, minio-storage 3 keer OK, nul validatiefouten. De laag is bewust de keuze van de componerende service; er wordt nergens aangenomen dat het deployment-niveau is.
- [x] Twee blokkades die het kloonpatroon herbruikbaar maakten. OPGELOST 1 augustus (niet uitgerold). Ten eerste `revision_manager`: `_get_service_config` en `_ensure_service_entry` gebruikten een JSONPath op `@.reference`, en drie andere queries (`get_active_revision`, `add_action`, `get_superseded_resources`) hadden elk hun eigen kopie daarvan. Alles loopt nu via `service_entry_name` en `service_entry_config`, dus alle vier de entry-vormen worden gevonden; de drie queries leunen op `get_revisions` zodat de vormafhandeling op één plek zit, en de twee JSONPath-helpers zijn daarmee dood en verwijderd. `_ensure_service_entry` hergebruikt nu een bestaande entry en promoveert een kale string in plaats van een tweede entry toe te voegen; dat laatste zou een duplicaat in de selectieset opleveren en dan wijst `validate_project_structure` het hele bestand af. Ten tweede het schema: `$defs/deployment-service` stond op `additionalProperties: false` met alleen `reference` en `config` (dus geen `name`-record en geen `schema-version`), en `$defs/deployment-service-config` liet alleen `generation` en `revisions` toe. Allebei opengezet tot envelope, met de reden in een `comment`. Omdat de globale schemacontrole daarmee wegvalt, loopt `validate_service_configs` nu ook over de deployment-laag, dus per-service modellen nemen het over. Geverifieerd tegen alle 47 productiebestanden na migratie: nul schemafouten, nul configfouten, en 33 revisies gevonden via de nieuwe lookup.
- [x] Alle services hebben nu een configmodel en een vastgelegd schemafragment. AF 1 augustus (niet uitgerold). `redis` (`acl-key-prefix`), `publish-on-web` (`tls` plus `attachment` op component en het `domains`-goedkeuringsblok op project) en `attachments` (lijst van koppelingen op component, als `RootModel` net als storage) hebben nu elk een `config_model.py` plus fragment. Dertien van de vijftien services zijn gemodelleerd; `namespace-redis` en `platform` blijven bewust behaviour-only. Ook `config_schema_version` rechtgetrokken: de default op de basisklasse was `"1.0"`, dus elke service zonder model adverteerde een contract dat er niet was; die is nu `None` en `tests/test_service_config_schema.py` bewaakt dat model en versie samen reizen. Geverifieerd tegen alle 47 productiebestanden na migratie: nul schemafouten, nul configfouten, en elke service valideert. Drie bestaande tests hadden een achterhaalde premisse (ze gebruikten publish-on-web of minio als voorbeeld van een service zónder model) en zijn omgezet naar `namespace-redis`, telkens met een test voor de omgekeerde kant erbij.
- [x] `validate_service_configs` loopt nu over alle vier de lagen. AF 1 augustus (niet uitgerold). Aanleiding: bij het openzetten van `$defs/deployment-service-config` had ik zelf een gat geslagen, want `persistent-storage` op deployment-component valt onder `additionalProperties` en werd daar dus alleen door dat gesloten object gevalideerd; een onzinsleutel glipte er daarna doorheen. Gemeten en bevestigd, en nu gerepareerd door de deployment-component-laag mee te lopen, met beide vormen die daar voorkomen: een dict gekeyd op servicenaam en, voor de storage-services, een lijst van per-mount-records onder die sleutel. Daarvoor was een contract-uitbreiding nodig: `Service.config_model_for(layer)` met `config_model` als default, want persistent-storage en temp-storage dragen mount-specs op component maar per-mount kloonstatus op deployment-component, en één model kan die twee niet beschrijven. Die twee services overriden hem naar `StorageCloneState` in `catalog/shared/storage.py`, dat op de gedeelde `CloneState` leunt. Geverifieerd: 47 productiebestanden nul fouten, 37 deployment-component-configs worden nu daadwerkelijk gevalideerd, en de regressie wordt gevangen.
- [x] `project_v2.json` opgeruimd nu de servicevalidatie sluit. AF 1 augustus (niet uitgerold). `deployment-component.services` noemde `attachments` en `publish-on-web` bij naam en is nu een generieke envelope (`$defs/deployment-component-service`) die de twee vormen toestaat die daar voorkomen: een record met een config, en een lijst van per-mount-records. Daarmee werden `$defs/publish-on-web-config` en `$defs/attachment-use-entry` wees en zijn ze verwijderd; die kennis zit nu in `PublishOnWebConfig` en `AttachmentsConfig`. De drie tests in `tests/test_attachment_schema.py` die tegen de verwijderde def valideerden zijn meeverhuisd naar `AttachmentUse`, met dezelfde regels. BEWUST BLIJVEN STAAN, met de reden in een `comment` in het schema: de root-`domains`-defs en de v0-boolean `publish-on-web: true`, want `git_monitor.py:137` valideert een bestand rechtstreeks uit git VOOR enige migratie, dus die vormen moeten daar door de poort komen; weghalen zou die beveiligingspoort legacy-bestanden laten afwijzen. Er staan nu nul servicenamen als contract in het schema. Geverifieerd op beide paden: 47 productiebestanden na migratie nul fouten, en rauw exact dezelfde 22 fouten als met het originele schema, dus niets nieuws kapot.
- [x] Een schema per versie. AF 6 augustus (niet uitgerold, PR #33). Het projectschema was één bestand voor de hele 2.x-reeks, en `git_monitor` valideert een bestand rechtstreeks uit git VOOR enige migratie; daardoor moest elke oude vorm blijven staan zolang er één onverwerkt bestand op schijf stond en kon een migratie nooit afgesloten worden. Nu: `project_v2.json` beschrijft alleen de nieuwste versie (annotatie `x-zad-schema-version`), en een oudere versie wordt samengesteld uit dat schema plus een keten van RFC 7386 merge-patches in `opi/schemas/project_legacy/`. De poort valideert tegen de versie die het bestand zelf declareert; ontbrekend, niet-numeriek of onbekend is een weigering, zonder terugval op het nieuwste of het soepelste schema. `check_schema_versions` vergelijkt bij het opstarten de migratieketen (`SCHEMA_VERSIONS`/`MIGRATION_STEPS`) met de schema's op schijf, dus een migratie zonder schema stopt de boot. Daarmee kon het nieuwste schema 161 regels legacy kwijt: root-`domains:`, root-`invites:`, component-`uses-services`/`storage` en de v0-boolean `publish-on-web`. Gemeten over de 47 productiebestanden: 47/47 valideren tegen hun eigen versie (was 25/47 tegen het nieuwste) en 47/47 na migratie tegen het nieuwste. Bewuste keuze: GEEN sweep die alles migreert, want oude versies mogen bestaan nu hun schema bestaat; zie `features/project-schema-versions.md`.
- [x] AF (5 augustus gemeten, de oude tekst klopte niet meer): de vijf genoemde services hebben inmiddels wél een configmodel. Zonder model resteren nog `namespace-redis` en de drie systeemdiensten `platform`, `resource-tuning` en `deployment-health`, en voor een systeemdienst zonder gebruikersconfig is dat geen gat. Alleen `namespace-redis` is nog de moeite van het nakijken waard.
- [x] RC-33 AF EN GEMERGED (6 augustus): config bewerkbaar waar hij hoort. `scope` is hernoemd naar `binding` omdat het niets zei over waar je config staat, en een dienst zonder projectsectie zegt nu waar hij wel in te stellen is.
- [x] RC-26 AF EN GEMERGED (5 augustus): één weg voor hoe een wizard met velden omgaat (`plans/wizard-onbewerkbare-velden-op-een-plek.md`). De flow verklaart nu zijn doelwit in plaats van het uit zijn eigen naam terug te rekenen, er wordt alleen geschreven wat de editables van de flow noemen (`opi/forms/wizard/write_set.py`), en de API-validatie komt uit dezelfde editables. Bijvangst diezelfde dag: drie lezers vonden serviceconfig niet in de wizard omdat die onder de virtuele sleutel staat; dat is bij de bron opgelost in `smart_get_value`.
- [x] RC-27 AF EN GEMERGED (5 augustus): elke service heeft nu een uitgebreide beschrijving in een eigen Jinja-template, achter het vraagteken op de servicekaart. Gemeten: nul services zonder `help_template`. Het serviceblok komt uit één macro (`service_block`), gebruikt door het serviceoverzicht, de wizard-picker en sinds die dag ook de projectpagina.
- [x] RC-13 AF EN GEMERGED (5 augustus geverifieerd): invites zijn een echte service (`opi/services/catalog/invite/` met `InviteService`, configmodel, editables, schemafragment en een eigen detailsectie). Op 5 augustus is de wizardkant daar nog flink op bijgewerkt: de rollenkiezer werd gevuld, de bestemming wordt per deployment en component gekozen in plaats van als URL getypt, verplichte velden zijn voorgevuld en de lijst is op één uitnodiging vastgezet.
- [x] RC-34 AF EN GEMERGED (6 augustus): één server-gerenderde voortgangsweergave. project-progress.html.j2 ging van 579 regels met 42 JavaScript naar 42 regels met nul. Bijvangst: een tweede template-injectie gevonden en gedicht in de wizard-modal.
- [x] `testcontainers` ontbrak in de venv waardoor vijf testbestanden niet konden laden. OPGELOST 1 augustus: `uv sync --all-groups` installeerde `testcontainers`, `docker` en `pytest-timeout`. Daarna bleek de oorzaak dieper te liggen: de reaper-container `testcontainers/ryuk` start niet op Docker Desktop (500 van de daemon) en nam elke containertest mee, 58 errors. `tests/conftest.py` zet nu `TESTCONTAINERS_RYUK_DISABLED` via `setdefault` in de `_orm_pg_container`-fixture, met de reden erbij; de container wordt sowieso door de context manager gestopt, dus de reaper is daar overbodig. `setdefault` zodat CI hem terug aan kan zetten als het daar wel werkt.
- [x] Het hele projectbestand, inclusief wachtwoorden, komt in de logs terecht. GEFIXT 1 augustus (niet uitgerold): `bridge.py` logt nu providernaam plus `sorted(kwargs)`, dus alleen sleutelnamen, en `import logging` plus de logger staan op moduleniveau in plaats van binnen de `try`. Regressietest `TestResolveOptionsDoesNotLeakSecrets` in `tests/test_editables_bridge.py`, geverifieerd dat hij faalt op de oude regel.
- [x] AF (6 augustus gemeten): het log-lek is als klasse gedicht. Geen enkele logregel in `manager/` of `generation/` dumpt nog een hele config-dict; `minio_manager` logt alleen nog sleutelnamen. Bovendien hebben minio en redis inmiddels allebei een `config_model.py`, dus die configs zijn geen losse dicts meer.
- [x] RC-31 AF EN GEMERGED (6 augustus): een uitgeschakelde deployment heet niet meer gezond, op alle drie de plekken (dashboardbanner, deployment-kaart, V2 API). De eis dat een echte storing niet verdwijnt achter "uitgeschakeld" is getest aan beide kanten (`test_a_real_failure_survives_being_switched_off` en `test_a_real_failure_outranks_being_switched_off`). LET OP bij de release-aantekening: een API-client die op `Healthy` filtert krijgt nu een andere uitkomst.
- [x] AANGESLOTEN 6 augustus: `order_deployments_by_clone_dependency` wordt nu toegepast in `ProjectManager.get_deployments`, de ene deur waar elke aanroeper doorheen komt, en na de filters zodat het de deployments ordent die je ook echt gebruikt. Het was dode code: de module en 85 regels tests bestonden sinds 3 augustus maar niets importeerde ze, dus klonen werden nog steeds in bestandsvolgorde verwerkt terwijl alles eruitzag alsof het geregeld was. Er staan nu drie tests op de bedrading zelf, want dat is precies wat ontbrak.
- [x] AANGESLOTEN 6 augustus: klonen worden nu na hun bron verwerkt. `order_deployments_by_clone_dependency` wordt toegepast in `ProjectManager.get_deployments`, de ene deur waar elke aanroeper doorheen komt, en na de filters. Het was dode code: module en tests bestonden sinds 3 augustus maar niets importeerde ze. Er staan nu drie tests op de bedrading zelf.
- [x] AF EN GEMERGED als RC-42 (7 augustus): inclusief de wizard, de API en een ketentest die aantoont dat een via de wizard ingevulde regel een compleet NetworkPolicy-document oplevert, met de per-deployment patch en een onbekende peer erbij.
- [x] RC-36 AF EN GEMERGED (7 augustus): alles van een dienst in een map. Geverifieerd met merge-base: de branch zit volledig in deze branch.
- [x] AL GECORRIGEERD (7 augustus gemeten): regel 25 zegt nu 'one package per service since RC-5'. Er was niets meer te doen.
- [x] AL BINNEN (7 augustus gemeten): die zes commits bevatten nul code- of configbestanden, alleen vijf markdown-documenten, en de inhoud daarvan zit al in deze branch in een NIEUWERE vorm (vlam.md 636 regels hier tegen 606 daar; vonk.rijksapp.dev, de ACL en het uitzetten van log-upload komen in beide even vaak voor). De branch loopt achter; mergen zou alleen oudere tekst terugbrengen. De slaapstand-code zat er allang in, vandaar dat de slaapstand gewoon werkt.
- [x] PRAKTISCH AF (7 augustus gemeten): van de laatste 200 commits heeft er NUL Claude als auteur of committer. Alleen in oudere commits op deze branch staat het nog, dus het is historisch en begrensd.
- [x] RC-39 AF EN GEMERGED (7 augustus): twee event-lijsten, een manier van inhaken. ActionEvent en UIEvent bestaan als losse enums, de drie eenmalige haken zijn van de basisklasse verdwenen en vervangen door elf @on-handlers, en het contract dat een actie-haak niet zelf committeert wordt bewaakt door een test die de broncode van elke handler inspecteert.
- [x] RC-40 AF EN GEMERGED (7 augustus): typecontrole op aanroepen aan. Beide pyright-vlaggen staan op true, nul type-ignores in productiecode, twaalf echte fouten gevonden. Een daarvan zelf nagespeeld: has_deployments_for_current_cluster nam geen argument terwijl git_monitor er het projectbestand aan meegaf, dus elk projectbestand met deployments liep daar op een TypeError.
- [x] RC-38 AF EN GEMERGED (7 augustus): diensten ontsluiten volgens definieren, gebruiken en binden. De 64 KB-grens staat op een plek en wordt ook door de wizardroute gehandhaafd, API-velden hergebruiken hun editable, POST/PUT/upsert zijn als 409/404 geimplementeerd, en een kapotte bijlagecatalogus wordt nu echt geweigerd.
